# Review: Frontend

## Scope

The web app, the brand package and the Keycloak sign-in theme. Paths, as [`areas.json`](areas.json) lists them:

- `apps/web/**`
- `packages/brand/**`
- `apps/keycloak-theme/**`
