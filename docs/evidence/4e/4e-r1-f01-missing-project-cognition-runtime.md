# 4E-R1-F01 — Missing R1 Project cognition runtime selection

> **Status:** `CLOSED FOR PLANNING / MASTRA STRUCTURE + MECHANICS ACCEPTED / PRODUCTION REPIN GATE DEFERRED`
> **Stage:** `4E(R1) COMPOSED COHERENCE`
> **Implementation authority:** `BLOCKED`
> **Product/operation change:** `0`

## 1. Finding

The exact R1 operation map contains two Project-owned cognition consumers:

```text
PRJ-07 RunInceptionInvestigation
PRJ-24 AskConexusAboutBaselineCandidate
```

R1 Foundation selected and proved profile compilation, strict admission,
canonicalization, Hub/OIDC/session, browser projection, PostgreSQL and
complementary gates. It did **not** select the exact model adapter/provider,
model-policy resolution, bounded call/step/retry/fallback envelope, credential
custody or result/provenance mechanics required to realize these two operations.

Therefore the composed R1 flow cannot yet be closed truthfully:

```text
Project creation
→ PRJ-07 cognition produces immutable Baseline candidate
→ PRJ-23 exact candidate review
→ PRJ-24 candidate-bound contextual explanation
→ PRJ-09 exact-digest approval
```

The gap is planning, not implementation failure. No Product code exists and no
runtime was executed.

## 2. Reproduction against current authority

1. The derived R1 map fixes exactly `13` operations and includes `PRJ-07` and
   `PRJ-24`.
2. `PRJ-07` is a Project-owned investigation/proof operation producing an
   immutable candidate; it cannot publish authority directly.
3. `PRJ-24` is a Project-owned, exact-candidate-bound read/assistant interaction
   under `project.manage`; its answer is read-only with provenance.
4. The Permission and W-01 owners explicitly forbid requiring Builder
   `project.build` merely because contextual assistance is visible.
5. The exhaustive 4D-04 runtime-family map contains Builder Agent runtime only
   at `RF-09/RB` and Product-Agent runtime only later; it contains no R1
   Project-cognition/model-adapter family.
6. The approved R1 Foundation selection contains no model/provider adapter or
   cognition execution boundary.
7. Realization Planning routes `3N-V17` bounded **Builder** model execution to
   RB; that later proof cannot satisfy an earlier Project-owned R1 consumer.

## 3. Why the finding survives

The following apparent shortcuts are invalid:

| Shortcut | Disposition | Reason |
| --- | --- | --- |
| borrow BuilderMastra/RF-09 for PRJ-07/24 | `REJECT` | RB is later and Builder authority/identity cannot become Project Baseline authority |
| call a provider directly without selection | `REJECT` | destination, credential, model policy, limits, provenance and failure semantics would be ambient implementation truth |
| return static/mock explanation | `REJECT` | cannot prove the accepted contextual cognition behavior and real model boundary |
| remove PRJ-07/24 from R1 | `REJECT` | would change the operator-approved operation reachability/first-slice Product contract |
| mark the composed flow PASS from Pack C | `REJECT` | Pack C proves HTTP/OIDC/session mechanics, not Project cognition |

This is a material 4E composition falsifier: one reachable consumer has no
admitted runtime family, exact mechanism or 4D-D firing claim.

## 4. Smallest owner reopen

The finding does not reopen Product meaning, the 128-operation ledger, 4B wire,
locked W-01 UX, the accepted R1 Foundation rows or unrelated runtime families.

The operator-approved minimum correction route is:

```text
4D-01/03 affected FE-08 owner routing only
→ classify Project-owned bounded cognition as an owner-local adapter, not a runtime family
→ 4D-C exact comparative selection for that owner-local adapter
→ affected 4D-D(R1) amendment with cognition claim + firing controls
→ resume 4E(R1) composed coherence
```

The family must preserve:

- Project owns Baseline candidate, question subject and answer provenance;
- model/runtime/provider identity is correlation/mechanics, never Product truth;
- `project.manage` does not become `project.build`;
- exact server-resolved Project/candidate/model-policy context;
- finite server-derived call/step/retry/fallback limits;
- server-only provider credential and destination custody;
- explicit partial/refusal/provider failure without candidate mutation;
- no conversation/thread/memory object becoming Baseline authority;
- no generic assistant, Agent fleet, workflow or Product-Agent runtime.

## 5. Selection questions, not decisions

The reopened selection must compare at least:

```text
narrow direct owner-local Model adapter
vs bounded Mastra mechanism behind the Project owner
vs another credible current minimal adapter discovered from primary sources
```

No candidate is selected by this finding. Mastra-sensitive work must use the
repository Mastra skill and exact current installed/source/official Evidence;
all library/framework/API claims require Context7. A package feature cannot
become Product authority.

The comparison must independently decide:

1. whether PRJ-07 and PRJ-24 can safely share mechanics without creating a
   universal assistant owner;
2. exact model/provider/policy selection and replacement boundary;
3. structured input/output/provenance envelope;
4. timeouts, retries, fallback and cancellation semantics;
5. secret/config/egress custody;
6. deterministic and real-model Evidence, including malformed/partial/refusal,
   stale-candidate and authority-narrowing controls;
7. version/upgrade/reopen law and affected R1 conformance claims.

## 6. Impact on approved 4D-D(R1)

The existing foundation claims `R1C-01..12` remain valid for their exact
subjects. The explicit 4D-D reopen trigger “an R1 path is unguarded” has fired
for Project cognition.

After the missing mechanism is selected, the smallest amendment adds an
independent cognition claim; it does not rewrite unrelated conformance rows.
At finding time the state was:

```text
4D-D(R1) foundation claims = preserved
full 13-operation 4D-D(R1) completeness = REOPEN REQUIRED
4E(R1) = STOPPED
4F(R1) / 4G(R1) / execution grant = BLOCKED
```

## 7. Operator decision

```text
ACCEPT 4E-R1-F01 / REOPEN AFFECTED 4D-04 + 4D-C + 4D-D(R1)
REVISE <exact finding/reopen scope>
HOLD
```

Acceptance authorizes planning/research only. It does not authorize package
installation, probe execution, Product implementation, model-provider calls,
live/paid/production effects, push, PR or merge.

The operator-approved Global-Maximum structure is recorded in
[Project cognition Global Maximum adjudication](4e-r1-f01-project-cognition-global-maximum.md).
That historical STOP is closed. The operator-approved ProjectMastra structure,
P13 Evidence and `R1C-13` addendum now route the cognition slice to 4F; exact
safe pin/provider admission remains a pre-implementation firing gate.
