# 4D-01R — Consolidated Strategic Opportunity Adjudication

> **Status:** `4D-01R CLOSED / OPERATOR APPROVED / GLOBAL-COHERENCE CORRECTIONS APPLIED / INDEPENDENT RECHECK CLEAR`
> **Scope:** OPP-A01..A04, OPP-B01..B07, OPP-C01..C09 and C02R
> **Implementation authority:** `BLOCKED`
> **Technology-selection authority:** `NONE`

## 1. Decision question

Did the complete strategic-opportunity challenge produce one consumer-backed,
owner-coherent and falsifiable 4D protected-property ledger that preserves
valuable future seams without selecting technology, inventing Product authority
or forcing first-slice machinery with no consumer?

## 2. Consolidated census

After deduplicating `VER-07` into `TEL-01`:

```text
opportunity studies                      = 20
Builder correction/adjudication          = 1 / C02R
protected properties                     = 117 unique
families                                 = 18

REALIZE                                  = 96
PRESERVE_SEAM                            = 18
DEFER                                    = 3
STOP                                     = 0

PLATFORM-CONTRACT                        = 101
APP-OWNED                                = 11
GENERATED                                = 5

exact technology selections              = 0/21
new Product owners/operations/records     = 0
upstream Product/architecture reopens     = 0
Product implementation authority          = 0
```

`STOP=0` means no unresolved prerequisite blocks the ledger candidate. Firing
falsifiers may still stop a claim/run at realization time without becoming a
ledger `STOP` disposition.

## 3. Final opportunity disposition matrix

| Study | Protected result | Candidate/alternative posture | Deferred/rejected boundary |
| --- | --- | --- | --- |
| A01 | FE-09 Product Agent experience road | generated client + bounded React/headless; assistant-ui/AI SDK alternatives | direct browser Mastra authority rejected; no forced shell |
| A02 | tool-neutral generated-wire projection oracle | Kubb TS/Fetch leading; React Query/Zod probes; Orval fallback | generator self-verification and parallel DTO/schema authority rejected |
| A03 | SCF-09/10 + VER-10 traceability/impact/drift | minimal Conexus compiler/manifest; bounded detectors | editable universal graph/Traceability owner rejected |
| A04 | FE-10 owner adapters + safe rendering kernel | exact source/version/anchor projection mechanics | universal Review owner/API/store/lifecycle rejected |
| B01 | generated route/schema/Problem boundary | Fastify leading; Hono challenger; Express fallback | Nest F1 rejected; custom only on framework falsifier |
| B02 | CR-1 and Project/Hub data integrity | `pg` + narrow I&A guard leading; migration challengers open | broad cross-owner SQL/ORM authority rejected |
| B03 | owner JobRun + queue subordination | pg-boss incumbent; Graphile challenger | Temporal/pg_durable current MAR adoption rejected |
| B04 | Sankhya Gateway/API provider boundary | official APIs + qualified bounded DB Explorer | Oracle-as-Sankhya, generic proxy and inferred SCN rejected |
| B05 | profile compiler + ownership manifest | Nx bounded leader; Copier update challenger | Backstage waits for real platform consumer; ad hoc templates rejected |
| B06 | layered supply-chain Evidence | npm current-repo incumbent; CycloneDX/SLSA leading semantics | generic governance/auto-admission/merge rejected |
| B07 | Project Release/platform deployment/environment separation | local CAS/systemd/OCI leading; Compose/Quadlet challengers | Kubernetes/Nomad current F1 rejected |
| C01 | DXE-01..03 durable-runtime seams | owner-local + pg-boss current baseline; strategic runtimes preserved | generic Workflow owner/current adoption deferred |
| C02 | MEM-01..04 + LRN-01 | reconnect current; memory mechanisms/evals consumer-gated | memory/Brain collapse and self-modifying learning rejected |
| C02R | real first operational Builder | native Mastra incumbent versus SDK/ACP mandatory challenger | simple chat/hand-built proving app rejected as Builder proof |
| C03 | CMP-01..08 + IOP-01..07 | capable implementer + fresh verifier; ACP/private-MCP alternatives | speculative fleet, Networks and internal A2A rejected |
| C04 | EVA-01..07 + TEL-01..05 + LRN-02 | private Worker Eval; OTLP seam; later backend bakeoff | Trace Intelligence deferred; score/trace never acceptance |
| C05 | DPL-01..06 governed Project data path | native/dlt and native/dbt/SQLMesh comparisons | ad hoc sync/universal Data owner/Debezium-for-Sankhya rejected |
| C06 | SCF-11 deny-only constraint seam | JSON Schema + owner-local baseline; OPA/CEL triggers | generic Policy/Compliance owner; Cedar/OpenFGA rejected |
| C07 | safe first-real-brownfield Evidence Map | deterministic baseline; syntax/SCIP/AI claim-triggered candidates | fixed rubric/composite readiness score deferred/rejected |
| C08 | enriched SCF-08 DEDICATED seam | real-consumer deployment options; future outbound connector hypothesis | DEDICATED≠private reachability; tunnel/VPN authority rejected |
| C09 | FE-06/07 refinement + FE-11 two-tier visual road | React Aria/Base UI prototypes; Radix control; styling/token challengers | one shell/brand, component authority and Ark Plus distribution rejected |

No row above is an exact package/version/topology selection. `Leading`,
`incumbent`, `challenger` and `reference` are 4D-C comparison inputs only.

## 4. Current first-slice composition after C02R

The first operational Budget Analyzer now explicitly includes:

```text
manual Hub/bootstrap foundation
→ R1/R2 owner foundations
→ RB real Builder
   Change / contract_revision / plan_revision / WorkUnit / ActorRun
   CodingSession / Finding / change_acceptance
   BuilderMastra + guarded E2B + Git/result custody
   capable implementer + fresh independent verifier
   private Worker Eval
→ Builder-authored R3–R5 Project application
→ R6 Release/Promotion/SERVED_VERIFIED
→ R7 real Sankhya/result/oracle proof
```

`bld.*` is therefore present in the first-slice durable authority budget and
`mastra_builder` is an exact Builder substrate. ParMastra/Product Agent,
`obs.*`, external effects, advanced memory/durable Agent/EVENT and first-
production recovery remain absent or preserved according to their ledger rows.

## 5. Observation/telemetry boundary

`TEL-01/02 REALIZE` means owner-local ordinary Builder/MAR observation and
evaluation Evidence only. It creates no `obs.*` Product path. `TEL-03..05` and
`VER-08` retain exporter/redaction/feedback/audit seams for their first real
consumers. `VER-07` was removed as a duplicate of `TEL-01`.

## 6. Serial Builder and future composition boundary

`CMP-06 REALIZE` now proves the current serial baseline and absence of concurrent
writer machinery. Parallel composition requires a measured-benefit reopen and
then exact non-overlap, lineage isolation, deterministic Hub order and conflict
stop.

`RUN-08` owns only dormant Product-Agent runtime-family absence. `CMP-08` alone
owns Product-Agent/external-agent composition admission. The two reopen routes no
longer overlap.

## 7. A02/A04 closure

- `SCF-09` now includes the tool-neutral canonical-wire manifest/census used to
  judge generated output independently of the selected generator;
- `FE-10` now includes exact source/version digest, deterministic projection,
  versioned anchors, explicit stale/forged failure, sanitization/link safety and
  unknown-content fallback.

## 8. Technology-selection boundary

4D-01R admits properties and comparison sets only. Exact selections remain at
4D-C after 4D-A/B close the owning properties. In particular:

- npm is the current repository incumbent, not yet the universal generated-
  Project package-manager selection;
- SLSA is leading provenance semantics, not a claimed level or admitted tool;
- Mastra/ACP, Fastify/Hono, pg/query/migrations, pg-boss/challengers, dlt/dbt/
  SQLMesh, design-system candidates and deployment transports all remain open at
  their exact rows;
- no live Sankhya/provider effect, Product code or production topology is
  authorized by the research.

## 9. Applied global-coherence corrections

The first fresh consolidation review returned `BLOCKED`. Lead accepted and
applied every material finding:

1. Builder durable classes and exact Mastra consumer were restored to Realization Planning and DAT-01;
2. CMP-06 was narrowed to current serial realization plus future measured reopen;
3. VER-07 was removed and RUN-08/CMP-08 were partitioned;
4. TEL-01/02 were routed as owner-local observation without `obs.*` instantiation;
5. B06 `ADOPT` wording was normalized to incumbent/candidate/leading semantics;
6. stale ledger/challenge/phase/test status and arbitrary row-count CI were removed;
7. FE-09 now requires an executed bounded contract prototype/negative matrix and later real-PAR proof for runtime claims.

Additional ledger audit corrections clarified SCF-02 Registry non-authority and
removed stale reconnect/composition wording from the deferred summary.

The independent recheck returned `CLEAR`: all seven findings are closed, the
targeted repository test is green and no new Product, authority, P13, Builder,
telemetry or deferred-seam contradiction remains.

## 10. Gate

The operator approved this candidate with one binding execution condition:

```text
global complexity remains planned
→ implementation proceeds in bounded increments
→ each increment produces usable internal value or an immediately consumed foundation
→ real company use/Evidence drives the next increment
```

The approval:

- ratifies the consolidated 4D-01 ledger and closes 4D-01R;
- includes completed A01-A04 outcomes through the consolidated approval without
  rewriting their historical study headers as separately approved;
- authorizes only the next planning step: 4D-A/4D-02 scaffold and ownership
  derivation according to the roadmap;
- keeps 4D-B, 4D-C exact selection, Product implementation, push, PR and merge
  separately blocked until their own gates.

It does not select a dependency or authorize implementation.
