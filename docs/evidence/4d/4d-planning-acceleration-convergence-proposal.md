# 4D — Planning Acceleration Convergence Proposal

> **Status:** `REVISION 2 / OPERATOR RATIFIED / 2026-08-29`
> **Authority:** none; operator ratification required
> **Implementation authority:** none

## Proposed route

0. Treat this as a material correction to the repository-local Phase-4 program,
   not a change to the Engineering Method and not a reopen of 4A/4B/4C.

1. Before code, create a minimum **bounding Evidence map** of all `128` Product operations with:
   operation ID, owner and first reachability in
   `R1 | RB | R2..R7 | CURRENT_LATER | NOT_INSTANTIATED`. Use it to bound each
   tranche and measure unnecessary-but-coherent scope. It remains derived from
   the operation ledger, owns no Product meaning and does not replace the later
   4F execution graph.

2. Replace one-family cadence with one `R1 FOUNDATION BATCH`: RF-01 remains the
   approved selection; select RF-02, RF-03, RF-04 and the RF-12 mechanics
   reachable by R1 together. The same batch explicitly closes the exact Node/G0
   runtime pin, package-manager/supply-chain admission and proportional
   format/type/build/test gates required by `VER-01`. RF-12A emits two
   independent decisions: JSON Schema Draft 2020-12 validation and RFC 8785
   canonicalization. Qualification `node_modules` presence is not dependency
   admission.

3. Exact-source selection precedes physical probes. After selection and before
   any implementation grant, request an explicit operator **probe grant** and
   execute isolated real qualification probes for: RF-01/RF-12 generation/
   canonicalization, Keycloak/OIDC, PostgreSQL owner isolation + CR-1 and
   browser/session negative paths. Each probe uses the existing c02r pattern:
   isolated directory, zero root/Product dependency, Evidence-only status and
   exact pins. Proof may falsify and reopen only the smallest affected selection.

4. Close the applicable 4D-D conformance/version/upgrade contract, 4E coherence,
   4F execution graph and 4G adversarial readiness only for the `R1` foundation
   tranche. Amend
   `docs/phases/4-implementation-readiness-program.md` and roadmap only after
   operator ratification so the gates are tranche-scoped. Full-composition 4E
   over the Budget Analyzer Golden Flow remains a standing obligation due no
   later than the tranche that makes R7/3O reachable.

5. Require a separate explicit execution grant for R1 foundation. R1 bootstrap
   is not the first operational Product proof. The first operational Budget
   Analyzer remains required to traverse RB Builder.

6. During R1 implementation, plan the RB RF-09/10/11 batch just in time, then R2
   and later slices. Each tranche inherits exact owners, real probes, scoped
   4E/4F/4G and an internal-company user/operator walkthrough before expansion.

7. `PRESERVE_SEAM` and `DEFER` remain machinery-free. Remove a mandatory large
   Global-Maximum Reference Review per commodity technology; use proportional
   research only for materially uncertain, irreversible or boundary-moving
   decisions.

8. Correct mutable-status duplication/guarding as a separate `LOCAL EXECUTION
   GAP`. Create a recoverable repository checkpoint before implementation, while
   push, PR and merge remain subject to explicit authority.

## Directed reviewer contract

Return exactly:

```text
ACCEPT | REVISE | REJECT
up to three material corrections
focused-day horizon to R1 eligibility
what remains JIT
terminal brief verdict
```

Test whether this is the smallest sustainable executable route and whether it
weakens authority, security, Builder, Release or Evidence. Do not edit files.
