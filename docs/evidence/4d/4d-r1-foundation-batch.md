# 4D — R1 Foundation Batch

> **Status:** `SELECTION CLOSED / INDEPENDENT REVIEW CONVERGED / OPERATOR APPROVED / 2026-08-30`
> **Program:** `BOUNDED PROGRAM COMPRESSION / OPERATOR RATIFIED`
> **Probe authority:** none
> **Implementation authority:** none

## 1. Objective

Close one coherent foundation selection/conformance package required by the R1
consumer:

```text
Keycloak OIDC authentication
→ Conexus Account/session/current authority
→ Workspace + Project + approved Baseline/access
→ exact generated profile + browser host
→ owner-isolated Hub persistence
→ deterministic verification/admission
```

RF-01 remains the approved `BUILD ConexusProfileCompiler/v1` selection. This
batch selects only its immediate prerequisites and the rest of the R1 foundation.

## 2. Batch contents

| Row | Decision scope | Why batched |
| --- | --- | --- |
| `R1F-01` | exact Node runtime/build-environment pin + package-manager/registry/install-script/supply-chain admission | shared execution/provenance root for every R1 mechanism |
| `R1F-02` | RF-12A JSON Schema Draft 2020-12 validator | RF-01 profile/input admission prerequisite |
| `R1F-03` | RF-12A RFC 8785 canonicalization implementation | RF-01 deterministic digest prerequisite; separate verdict from validation |
| `R1F-04` | RF-02 Hub request/router + confidential Keycloak OIDC client/session/current-authority mechanics | one R1 server/security trust boundary |
| `R1F-05` | RF-03 browser host, generated transport, routing/query/state and same-origin security foundation | consumes the same R1 wire/session boundary |
| `R1F-06` | RF-04 PostgreSQL driver/query/owner isolation + narrow CR-1 serialization primitive | one R1 persistence/current-authority proof boundary |
| `R1F-07` | RF-12 `VER-01` proportional format/type/build/unit/integration/security gates | common admission and firing-proof boundary for R1 |

One package does not force one outcome. Every material row records its own:

```text
protected property + consumer
→ exact official/source/version Evidence
→ credible alternatives
→ ADOPT | ADAPT | BUILD | DEFER | STOP
→ authority/replacement boundary
→ proof/firing obligation
→ upgrade/reopen law
```

## 3. Required batching discipline

- compare cross-row compatibility once rather than repeating context;
- do not let a commodity row lower RF-02/RF-04 security depth;
- RF-12 validator and canonicalizer have independent outcomes;
- installed/transitive/qualification package presence is not admission;
- no selection by incumbent, popularity or framework feature count;
- no other RF family enters this batch;
- exact pins may still be falsified by later authorized probes.

## 4. Post-selection probe boundary

Selection does not execute probes. After batch adjudication, the operator may
separately grant an Evidence-only qualification package:

```text
RF-01/RF-12 generation + canonicalization
Keycloak/OIDC/session/bootstrap real negative paths
PostgreSQL owner/store isolation + ordinary transaction capability
browser/session real negative paths
```

Each probe uses an isolated directory, zero root/Product dependency, exact pins
and no Product effect. Probe failure reopens the smallest selection. Probe
success does not grant implementation.

CR-1 concurrency remains selected as a protected contract but its first real
consumer is R6 `PromoteRelease`; implementation and race proof remain JIT for
R6, not part of the R1 probe grant.

## 5. R1 readiness after probes

Only after successful/adjudicated probes:

```text
applicable 4D-D(R1)
→ 4E(R1)
→ 4F(R1)
→ 4G(R1)
→ explicit operator R1 execution grant request
```

The exact R1 operation scope begins from
[the derived reachability map](4d-r1-operation-reachability-map.md). R1
foundation/bootstrap remains distinct from the first operational Budget Analyzer,
which still requires RB Builder.

## 6. Non-goals

No probe, code, dependency installation, Project schema, Product operation,
Builder runtime, Sankhya Gateway, sync, Release serving, push, PR or merge is
authorized by this planning batch.

## 7. Exact next action

Prepare one isolated R1 Foundation Probe Grant request over approved
`R1F-01..07`. It must name exact pin-manifest verification, probe directories,
real dependencies, negative controls, external effects, cleanup and Evidence
outputs. Preparing the request does not authorize probe execution, package
installation or Product code.
