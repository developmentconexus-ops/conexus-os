# 4D — Planning Acceleration and Executability Independent Review Brief

> **Status:** REVIEW BRIEF / NO VERDICT
> **Review target:** current Conexus planning program and remaining path to first implementation eligibility
> **Candidate:** local worktree over `HEAD 1eb096f6748c4bca45f096ac678e1a0902bab0c6`
> **Method:** Engineering Method + Repository Method + Blueprint Harness §10.4–10.6
> **Implementation authority:** none

## 1. Operator concern

The operator accepts rigorous planning but challenges its current duration and
risk of producing a theoretically elegant, over-complex “Godzilla” system that
is difficult to implement or learn from. The desired Product should exceed
Mitra/Factory AI in governed enterprise value, not in speculative machinery.

The operator prefers:

```text
globally coherent direction and future seams
→ smallest structurally sound base
→ useful internal operational increment
→ real Product/runtime/business Evidence
→ just-in-time expansion and correction
```

They ask whether decisions can be grouped or accelerated without sacrificing
material quality, and want an intelligible account of what planning remains and
how long it plausibly takes.

## 2. Repository bootstrap

Start independently from:

```text
AGENTS.md
→ docs/roadmap.md
→ docs/index.md
→ docs/development/engineering-method.md
→ docs/development/repository-method.md
→ docs/phases/4d-project-paved-road-and-runtime-realization.md
```

Then inspect at minimum:

```text
docs/phases/realization-planning.md
docs/phases/4-implementation-readiness-program.md
docs/evidence/4d/4d-01-protected-property-ledger.md
docs/evidence/4d/4d-02-project-scaffold-profile-and-ownership-contract.md
docs/evidence/4d/4d-03-paved-road-property-contract.md
docs/evidence/4d/4d-04-runtime-family-applicability.md
docs/evidence/4d/4d-05-rf01-profile-compiler-selection.md
docs/evidence/4d/4d-05-rf01-global-maximum-reference-review.md
```

Use exact owners or Mitra/Factory AI research only for a named uncertainty or
falsifier. Do not recursively load unrelated historical Evidence.

## 3. Current state

```text
4A = CLOSED
4B = CLOSED
4C = CLOSED / OPERATOR RATIFIED

4D-01R = CLOSED / 117 properties
4D-A/4D-02 = CLOSED / OPERATOR APPROVED
4D-B/4D-03 = CLOSED / OPERATOR APPROVED
4D-04 = CLOSED / OPERATOR APPROVED
RF-01 = BUILD SELECTED / OPERATOR APPROVED
RF-12A = exact next selection

4D remaining = exact reachable-family selection + conformance/escape/eval + global review
4E = whole-system coherence and Golden Flows
4F = implementation program/execution graph
4G = adversarial readiness
Product implementation = BLOCKED
```

The first operational target remains one Builder-produced read-only Budget
Analyzer through R1, RB and R2–R7. The accepted incremental-value law explicitly
forbids building all `REALIZE` properties simultaneously.

The worktree is not an immutable pushed checkpoint. Review current files
read-only and report provenance risk; do not edit, push, open PR or merge.

## 4. Decision question

> Is the current remaining planning program the smallest sustainable path to a
> high-quality executable Conexus foundation, or is it over-stopping and
> front-loading decisions that should be grouped, deferred or moved to
> implementation-time Evidence?

## 5. Required analysis

### A — Remaining-work inventory

List every material planning closure still required before implementation can
be eligible. Distinguish:

```text
MUST CLOSE BEFORE FIRST CODE
MUST CLOSE BEFORE ITS SLICE
PRESERVE SEAM ONLY
DEFER UNTIL OPERATING EVIDENCE
```

Do not assume every RF family needs an independent large decision document.

### B — Critical path and batching

Identify dependencies among remaining 4D/4E/4F/4G work. Propose the smallest
safe batches and which decisions can share one comparative review because they
have the same consumer, evidence and rollback boundary.

Explicitly challenge the current one-family-at-a-time cadence.

### C — Complexity and executability

Find:

- speculative framework/platform machinery;
- duplicate or overly granular contracts/gates;
- decisions that cannot be proven until implementation;
- abstractions justified only by other abstractions;
- places where Mitra/Factory AI demonstrate a materially simpler viable shape;
- places where their simplicity depends on authority/security shortcuts Conexus
  must not copy.

### D — Method review

Test both:

```text
OVER-STOPPING
planning/review/document ceremony delays useful Product learning
without protecting a material property

UNDER-STOPPING
acceleration would cross an unresolved owner/trust/irreversible boundary
that implementation cannot safely discover ad hoc
```

### E — Time/effort horizon

Give a realistic range, with assumptions, for:

1. remaining planning under current cadence;
2. remaining planning under your recommended batching;
3. earliest safe start of the first implementation tranche;
4. what planning continues just-in-time after that start.

Use decision batches and focused working days/weeks, not false precision.

### F — Recommended program

Produce a compact ordered route from now to first implementation eligibility.
For every step name:

```text
decision/output
why it must happen now
what is deliberately deferred
exit proof
```

## 6. Protected properties

Any acceleration must preserve:

- mechanism never becoming Product authority;
- owner/trust/security and current-authority boundaries;
- first real Builder-produced Budget Analyzer consumer;
- deterministic generated/platform/app-owned custody;
- real-dependency proof for real-dependency claims;
- Release/Promotion/SERVED_VERIFIED separation;
- no blind external-effect replay;
- incremental usable value rather than dormant platform breadth;
- implementation, push, PR and merge still requiring explicit authority.

## 7. Non-goals

Do not:

- implement or edit repository files;
- select RF-12A or another dependency merely to speed the review;
- reopen accepted Product/architecture decisions without a material falsifier;
- recommend copying Mitra/Factory AI authority or security shortcuts;
- treat reviewer agreement as authority;
- answer only with general process advice.

## 8. Finding contract

Classify findings as:

```text
METHOD FINDING
PRODUCT / PLAN GAP
LOCAL EXECUTION GAP
NO FINDING
```

For every material finding provide evidence/path, failure mode, root cause,
smallest owner/stage, recommendation, what must stop, what must not reopen and a
falsifier/proof.

End with:

1. current-cadence time horizon;
2. recommended-cadence time horizon;
3. exact decisions to batch/defer/delete;
4. smallest safe next action;
5. verdict:

```text
CURRENT PROGRAM CONFIRMED
BOUNDED PROGRAM COMPRESSION
MATERIAL METHOD CORRECTION
STOP / REOPEN OWNER
```

Reviewer output is Evidence. Codex Lead will cross-examine the reviewers and
adjudicate against repository authority before asking the operator to ratify any
program change.
