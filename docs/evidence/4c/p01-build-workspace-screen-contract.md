# P-01 — Project Build workspace Screen Contract

> **Status:** `LOCKED BASELINE / P12 FAMILY 1 RE-LOCKED / P12 FAMILY 4 AGENT-INGRESS DELTA RE-LOCKED / OPERATOR APPROVED`
> **Method:** Frontend Product Experience Planning Method v2.3
> **P9:** `P9 EXACT TRACE CLOSED`
> **P10:** `P10 CONSOLIDATED`
> **P11:** `P11 LATER ASSEMBLED PRODUCT`
> **Baseline operator-approved P8:** `docs/evidence/4c/p01-build-workspace-functional-wireframe.html`
> **Re-locked canonical P8:** same canonical HTML; no parallel Agent Studio artifact
> **Exact baseline identity:** `approved final P8 artifact blob = 3f9d30f7e6fa0de814f0be20b7583b4e98aca7c8`
> **Exact re-locked identity:** `approved Agent Studio P8 artifact blob = b679504046b7ef9956ede6757b1e9ebd9030770f`
> **Exact PRE11-F05 delta identity:** `approved Agent Studio reference-delta P8 artifact blob = 8ff34e12ab35ee69f8ffaff1bdd0a8274ac62cec`
> **P12 Family 1 approved identity:** `approved identity-custody P8 delta blob = e5782b3f9e828a5c247b405821589d52039cc179`
> **P12 Family 4 approved identity:** `approved Agent-ingress P8 delta blob = 25e5077106892c4ff6aba6774987e73a12ccff51 / OPERATOR APPROVED 2026-08-28`
> **Bounded upstream correction:** `4C-F15 / BLD-10 GetBuildPreview`
> **Whole-wire lock proof:** `Verify #860 SUCCESS` on the operator-locked P-01 authority/wire/Screen Contract candidate.

---

## 0. Bounded F28/F30 reopen disposition

The prior P-01 app-first shell, default Preview, right-side Conexus interaction, governed inspectors and read-only Code/Diff lenses remain the locked baseline. F28/F30 reopens only the missing specialized Product Agent authoring lens.

```text
P-03 exact Agent detail
→ Edit Agent
→ P-01 Agent Studio alternate Build lens
→ exact Project + agentId + authoredRevisionId + Release context preserved

inspection
→ PRJ-21 complete safe ProductAgentDefinition

Start editing
→ BLD-03 exact Change intent
→ BLD-19 explicit origin NEW | EXISTING + Idempotency-Key
→ ProductAgentAuthoringDraft

reload
→ BLD-18 exact current server draft

structured save / Conexus edit
→ BLD-20 expectedDraftRevision
→ same candidateSubjectDigest / Change diff / proof / Release path
```

Locked structural laws:

```text
default generic Build entry remains Preview + right Conexus
Agent Studio is a specialized alternate lens, not a second Builder shell
PRJ-21 inspection != project.build mutation authority
form state before submit = EPHEMERAL_UI
accepted draft/revision = SERVER
draft != live Agent != Release
structured edit + Conexus edit → same Change candidate
stale expectedDraftRevision → conflict + reload; never silent overwrite
Code/Diff remain read-only
capabilityId != Mastra/provider tool identity
instructions != provider system-prompt storage authority
```

The operator exercised the fresh-truth-revised HTML and explicitly approved it. The in-artifact candidate wording remains historical Evidence; current lock authority lives in this Screen Contract and the roadmap. The prior baseline and Agent Studio re-lock identities above remain historical P8 identities; the PRE11-F05 reference delta is pinned to its exact approved blob above.

Fresh P8 truth audit correction:

```text
URL-authored name/purpose/revision/Release trust = REMOVED
agentId/origin navigation coordinate → exact fixture re-resolution
selected Agent complete definition → every structured field
tool binding = exact capabilityId + independently authored purpose
sampling omission = preserved as policy default
NEW | EXISTING = both represented through BLD-19
diff = complete ProductAgentDefinition, not instructions/tools excerpt
```

This remains the same bounded Agent Studio reopen and the same canonical HTML. It creates no parallel document, editor, Agent database or runtime authority. The prior F28/F30 HTML identity is preserved as historical Evidence; the current PRE11-F05 delta lock below owns only reference selection/presentation.

`4C-PRE11-F05` resolves that upstream question and therefore reopened only the specialized Agent Studio reference-selection regions. The locked delta consumes purpose-bound `PRJ-16/17`, `BRN-14` authoring references and `PRJ-29`; NEW keeps unowned optional ref arrays empty, EXISTING preserves them exactly, and neither path accepts fixture/free-text refs as authority. The app-first Build root, right-side Conexus interaction, Change/diff/proof/Release path and all non-Agent Studio structure remain locked.

### 0.1 Agent Studio delta P9 exact trace

| Region/control | Owner read/write | Identity/concurrency | Client state | Material failure/success | Forbidden frontend authority |
| --- | --- | --- | --- | --- | --- |
| EXISTING entry | `PRJ-21` | exact Project + `agentId`; server re-resolves authored revision/Release | navigation coordinate only | non-disclosure remains distinct from absence/failure | URL-authored definition truth |
| NEW entry | `BLD-19 origin=NEW` | server-issued candidate Agent identity + Idempotency-Key | initial form is ephemeral | accepted draft becomes server-owned Change state | client-issued live Agent identity |
| Start editing EXISTING | `BLD-03` + `BLD-19 origin=EXISTING` | `expectedAuthoredRevisionId` + Idempotency-Key | form → accepted draft | stale/missing exact Agent fails closed; live Agent unchanged | direct Agent CRUD/source mutation |
| Complete definition form | `ProductAgentDefinition` inside `ProductAgentAuthoringDraft` | schema-closed `agent/v1` | unsaved values are ephemeral | required/interactions/tool-purpose validation before submit | provider passthrough, credentials or arbitrary extensions |
| Reload | `BLD-18` | exact Change + Agent draft | replaces local values with current server draft | current `draftRevision` restored | browser draft as authority |
| Save revision | `BLD-20` | `expectedDraftRevision` | accepted revision becomes server truth | conflict requires reload; no silent overwrite | last-write-wins mutation |
| Conexus edit | `BLD-20` through the same typed draft | same Change/draft revision | assistant text is not owner state | accepted revision appears in same candidate | second prompt-only Agent store |
| Review diff | `BLD-07` over same Change candidate | exact candidate lineage | read-only projection | complete definition is inspectable before Release | editable Code/Diff |

### 0.2 Agent Studio delta P10 consolidation

The re-locked delta reuses existing locked patterns only where semantics repeat:

```text
alternate Build lens                    → existing P-01 lens grammar
exact owner read before mutation        → locked inspect-then-act pattern
ephemeral form → server revision        → existing Change/Plan concurrency grammar
conflict → explicit reload              → no silent overwrite
human labels + exact refs secondarily   → P-02/W-04 progressive disclosure
```

No generic resource editor, form engine, Agent framework panel or new design system abstraction is admitted.

### 0.3 PRE11-F05 reference delta P9 exact trace

The operator-approved reference delta is confined to the existing Agent Studio lens. It consumes separate owner projections; the browser does not compose a universal reference catalog or treat a fixture/free-text ID as authoritative.

| Region/control | Owner read/write | Identity/concurrency | Client state | Material failure/success | Forbidden frontend authority |
| --- | --- | --- | --- | --- | --- |
| Model policy choice | `PRJ-29` `ListProjectModelPolicies` under purpose-bound `project.build` | exact Project + `policyRef`; `isDefault` is server-issued | selected policy and optional sampling are ephemeral until draft save | READY choices show label/purpose/limits; no valid default or denied/unavailable owner read blocks NEW `BLD-19`; owner `422` remains unresolved | provider/model/endpoint/credential/runtime override or client-owned default |
| Capability choices | `PRJ-16` summary and progressive `PRJ-17` detail | exact Project + `capabilityId`; authored binding purpose is separate form meaning | selected bindings and purposes are ephemeral until draft save | owner list/detail disclosure may be empty, withheld or failed without clearing exact EXISTING bindings; accepted draft validates references | invocation, business-data access, or Mastra/provider tool identity |
| Project Brain choices | `BRN-14` purpose-bound Project Brain context | exact Project + server-issued `authoringRef`; `conceptRef` is browse/presentation-only | selected authoring references are ephemeral until draft save | `detailDisclosed=false` renders withheld detail; sections/provenance appear only when disclosed; owner failure preserves exact refs | Workspace Brain browse/publication, runtime effective slice, or browser-composed context |
| Unowned optional references | preserved inside `BLD-18/19/20` `ProductAgentDefinition` | exact existing arrays; no semantic owner/read currently admitted | NEW empty; EXISTING read-only protected values | NEW displays “None configured; cannot be added in F1”; EXISTING survives reload/revision/diff; missing catalog != invalid | free-text policy/approval/budget/verification reference authority |
| Owner-state walkthrough | fixture simulation of the above owner reads | `LOADING`, `KNOWN_EMPTY`, `DENIED`, `NON_DISCLOSABLE`, `DEPENDENCY_FAILURE`, `READY` | local review control only | exact ref states remain `DISCLOSED`, `DETAIL_WITHHELD`, `EXACT_UNRESOLVED` or `SERVER_INVALID`; only owner `422` is invalid | one generic spinner/error or client-derived invalid/deleted state |

NEW remains:

```text
purpose-bound owner reads READY
→ server-issued PRJ-29 default selected
→ capability/authoringRef choices only from owner projections
→ unowned optional arrays empty
→ BLD-03 exact human intent + BLD-19 origin=NEW + Idempotency-Key
→ server-owned ProductAgentAuthoringDraft
```

EXISTING remains:

```text
exact agentId coordinate
→ PRJ-21 complete definition re-resolved
→ BLD-19 origin=EXISTING + expectedAuthoredRevisionId
→ BLD-18 current draft reload
→ BLD-20 complete definition + expectedDraftRevision
→ BLD-07 complete definition diff
```

Owner failure never clears a protected reference or silently falls back to a fixture/default. A stale `expectedDraftRevision` fails closed and requires reload; a reference validation `422` keeps the draft visible and unresolved.

### 0.4 PRE11-F05 reference delta P10 reconciliation

The delta reuses existing inspect-then-act, progressive technical disclosure, exact-subject/revision concurrency and explicit stale-reload patterns. It introduces no new shared production pattern (`P10 new graduated shared patterns = 0`) and no generic reference catalog, editor, form engine or frontend authority layer. The two canonical HTMLs remain disposable P8 Evidence; this lock does not authorize P11, 4D or Product implementation.

---

## 1. Locked human job

```text
enter Project → Build
→ see the current Project application immediately
→ converse with Conexus in the right sidebar
→ optionally reason in Plan mode without creating mutation work
→ state a Build instruction when something must change
→ Conexus/Builder creates exact durable Change work behind the interaction
→ keep the current / last-good application inspectable while candidate work progresses
→ inspect Code / Diff or governed Plan / Findings / Evidence / execution detail only when needed
```

Binding structural baseline:

```text
Build → current Project application immediately
Conexus chat = right sidebar
no active Change required
no Change chooser
Preview = default / dominant application lens
Code / Diff = read-only inspectable lenses
Plan / Findings / Evidence / Details = on-demand inspection
simple by default + inspectable by design
```

The user enters Build to build the application, not to administer internal Builder records. `Change` remains durable work truth but is not the root navigation object.

---

## 2. P9 — exact authority and interaction trace

### 2.1 Global / Project shell

P-01 inherits the already-locked GF-01 single adaptive rail and Project context. It does not create a second Builder shell or new Project IA.

```text
Workspace / Project context = existing GF-01 shell
Build route = URL_NAVIGATION
Project route = URL_NAVIGATION
```

### 2.2 Default Build entry — current Project application

```text
BLD-10 GetBuildPreview without changeId → CURRENT_PROJECT
Permission = project.build
subject owner = Builder / MAR Preview projection
server resolves = current canonical Project source
subjectDigest = exact server-resolved current Project source Preview subject
```

The browser does not select `sourceRevision`, derive “current” from latest Change, or substitute active Release serving state.

Binding laws:

```text
CURRENT_PROJECT != CHANGE_CANDIDATE
current Project source != active Release
Build Preview != Published App serving state
current != candidate
ready != verified != live
live = false on BLD-10
```

### 2.3 Build instruction — durable Change begins behind the conversation

```text
Build composer / BUILD mode
→ authored nonblank human instruction
→ BLD-03 CreateChange
→ Permission = project.build
→ exact Change established
→ Build instruction → exact Change.intent
→ changeId remains exact durable technical identity
```

The chat is the human interaction surface; `bld.change` is the durable work subject.

```text
chat = human interaction surface
Change = durable work truth
```

### 2.4 Candidate Preview after Change exists

```text
BLD-10 GetBuildPreview with optional exact changeId → CHANGE_CANDIDATE
Permission = project.build
changeId = untrusted exact Change reference
server re-resolves Project containment/current disclosure
subjectDigest = exact Change candidate Preview subject
```

The current / last-good application stays inspectable while candidate work is in progress.

### 2.5 Plan mode in the composer

Before a Change exists:

```text
Plan mode before Change = BLD-16 Project-context conversational reasoning
Plan mode before Change != BLD-04 durable ChangePlan
question only
→ project.build
→ current authorized Project/platform Builder context
→ read-only contextual guidance
→ no Change mutation
```

A conversational plan may help the human decide what to ask for, but it is not durable Builder Plan truth.

After a Change exists:

```text
BLD-04 GetChangePlan
→ project.build
→ exact current Change Plan projection

BLD-05 DecideChangePlanCheckpoint
→ project.review
→ exact Plan revision + current reviewer eligibility
```

### 2.6 Hub-owned progress

```text
BLD-06 GetChangeProgress
→ project.build
→ exact Change / Plan progress truth
```

```text
Hub progress != model narration
conversation != Progress truth
```

### 2.7 Code / Diff lenses

```text
BLD-07 GetChangeDiff
BLD-08 ListProjectSourceTree
BLD-09 GetProjectSourceFile
→ project.source.read
→ exact immutable/current source coordinates
→ read-only inspection
```

```text
visible Code/Diff != project.source.read grant
source-readable != source-mutable
```

Build can make these lenses discoverable without deriving or elevating source Permission in the browser.

### 2.8 Findings / Evidence / checkpoint review

```text
BLD-05 DecideChangePlanCheckpoint
BLD-11 ListChangeFindings
BLD-12 GetFinding
BLD-13 CloseFinding
BLD-14 ListChangeEvidence
BLD-15 GetEvidence
→ project.review
→ exact current review subject + server-resolved eligibility/disclosure
```

```text
visible Findings/Evidence != project.review grant
visible Plan decision != reviewer eligibility
no Findings != verified
Evidence present != claim proven
```

### 2.9 Contextual Conexus assistance

```text
BLD-16 AskConexusAboutContext
question only
→ Project-level Builder context

question + optional changeId
→ exact server-resolved current Change context

Permission = project.build
```

The optional Change reference does not acquire source, review or runtime authority.

```text
conversation != Change
conversation != Plan truth
conversation != Progress truth
conversation != verification
assistant answer != owner state
assistant answer -X-> Change / Plan / progress / verification truth
```

### 2.10 Execution detail

```text
BLD-17 GetChangeExecutionDetail
→ project.build
→ exact subordinate WorkUnit / ActorRun projection
→ progressive disclosure only
```

WorkUnit / ActorRun mechanics never become root IA or generic runtime-control surfaces.

---

## 3. Permission separation

This separation is binding across all P-01 surfaces:

```text
project.build != project.review != project.source.read
```

| Human job | Exact operations | Permission |
| --- | --- | --- |
| Current / candidate Build Preview | `BLD-10` | `project.build` |
| Create / inspect Change, Plan read, progress, assistant, execution detail | `BLD-01..04`, `BLD-06`, `BLD-16`, `BLD-17` | `project.build` |
| Code / Diff | `BLD-07..09` | `project.source.read` |
| Plan checkpoint / Findings / Evidence | `BLD-05`, `BLD-11..15` | `project.review` |

A visible tab, button, assistant reference or existing `changeId` never implies the other Permission families.

---

## 4. Client-state classification

```text
current/candidate Preview = SERVER
active Change after create = SERVER
Change intent / Plan / progress / Findings / Evidence / execution detail = SERVER
source tree / file / diff = SERVER
assistant response = SERVER read response; never mutation authority
composer draft/chat fixture = EPHEMERAL_UI
lens/inspector open state = EPHEMERAL_UI
Project route = URL_NAVIGATION
```

The P8 transcript is deliberately disposable local Evidence:

```text
Builder chat transcript in P8 = EPHEMERAL_UI fixture
durable Builder conversation/thread persistence = NOT ADMITTED
```

No `BuilderThread`, `BuildSession`, conversation memory owner, transcript persistence or generic AI-workspace record is introduced by P-01.

---

## 5. Material state distinctions

```text
loading != known-empty != denied != absent/non-disclosable != dependency failure
working != blocked != waiting-for-user != completed
current != candidate
ready != verified != live
```

Additional laws:

```text
last-good Preview survives while next candidate builds
candidate ready != verification
verification != Release
Release != live serving
```

P-01 must not flatten these into one spinner, one generic error, one “done” state or model narration.

---

## 6. P10 — consolidation

Existing graduated shared vocabulary before P-01:

```text
context-preserving exact-subject panel
```

P-01 instantiates that already-graduated pattern for on-demand Plan / Findings / Evidence / Details inspection while the application/Change context remains visible. It does not graduate a second generic panel abstraction.

```text
P10 new graduated shared patterns = 0
existing graduated patterns remain = 1
P11 = LATER ASSEMBLED PRODUCT
```

The following are intentionally single-instance P-01 composition, not shared production abstractions:

```text
app-first Builder + right-side chat = single-instance
Build/Plan composer = single-instance
Preview / Code / Diff lens group = P-01 composition
```

Do not invent generic `AIBuilder`, `AgenticWorkspace`, `BuildSessionStore`, `ThreadOwner`, universal IDE shell, cross-owner DTO or shared frontend authority layer from this lock.

---

## 7. Forbidden / not admitted

```text
Change chooser as root = FORBIDDEN
current app from latest Change = FORBIDDEN
current app from active Release = FORBIDDEN
browser-owned current app from source = FORBIDDEN
caller-selected sourceRevision for CURRENT_PROJECT Preview = FORBIDDEN
chat transcript as Product/Builder truth = FORBIDDEN
assistant answer as Plan/progress/verification authority = FORBIDDEN
source mutation in Code/Diff = FORBIDDEN
source permission elevation = FORBIDDEN
review permission elevation = FORBIDDEN
generic Change status editor = FORBIDDEN
WorkUnit / ActorRun root navigation = FORBIDDEN
generic AI Builder/session owner = NOT ADMITTED
durable Builder conversation/thread persistence = NOT ADMITTED
P11 early assembly = FORBIDDEN
```

---

## 8. Locked baseline and re-locked Agent Studio law

P-01 is locked to the operator-approved human experience and exact authority trace above. A later phase may reopen only the smallest affected owner if a material falsifier proves this experience cannot be implemented truthfully.

The lock does **not** authorize:

```text
P-02 opening
P11 assembly
4D start
Product implementation
PR #57 merge
```

Historical routing after the baseline lock:

```text
P-01 = LOCKED / OPERATOR APPROVED / P9/P10 CLOSED
P-02 = NEXT / NOT OPEN
```

Current routing after the operator-approved fresh-truth and PRE11-F05 re-LOCKs:

```text
P-01 = LOCKED / OPERATOR APPROVED
Agent Studio F28/F30 delta = RE-LOCKED / P9 EXACT TRACE CLOSED / P10 CONSOLIDATED
Agent Studio PRE11-F05 reference delta = LOCKED / P9 EXACT TRACE CLOSED / P10 CONSOLIDATED / blob 8ff34e12ab35ee69f8ffaff1bdd0a8274ac62cec
P-03 = LOCKED / OPERATOR APPROVED
next = roadmap-owned post-P-03 continuation; no implicit P11/4D/Product implementation
```
