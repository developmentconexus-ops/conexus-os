# P12 — behavioral P11 reassembly candidate

> **Status:** `P11 LOCKED / OPERATOR APPROVED / BROWSER GREEN`
> **Approved blob:** `45172fd437b0c3b0236b641bcb803c20f165959f`
> **Operator lock:** 2026-08-28
> **Historical locked P11:** `536052096dd10dec2f604ccef49aa64ba52e4dac` — preserved, not current proof
> **Product / 4A / 4B / 4D impact:** none

## Selected realization

```text
one active exact re-locked iframe
→ same-origin owner-issued child message
→ exact active-edge allowlist
→ destination-specific query serialization
→ destination owner resolution or fail-closed refusal
```

The parent owns only journey/step, mount state, validated Evidence envelope and
in-memory operator notes. It owns no business-coordinate defaults, authorization,
normalized Product state or cross-owner fixture relations.

## Removed defect class

- deleted the synthetic global coordinate registry and obsolete identities;
- deleted generic `P11_REVIEW_BOUNDARY` advancement;
- deleted button/text scraping and parent-triggered child tabs;
- deleted parent selection/hiding/stamping of Effect rows;
- deleted caller transport of `effectAttemptId`;
- removed visual highlighting that wrote style/data attributes into locked children;
- destination URLs now derive only from validated owner events.

## Behavioral Evidence

```text
13/13 exact mounts → one iframe each / exact title / marker load
Journey I → W-04 Agent event → P-03 owner resolution → P-01 AGENT_READY
Journey H → P-04 SERVED_RELEASE_READY → P-05 READY
          → explicit IAM-15 → PA-01 IAM13_READY
Journey K → P-03 approval-781/run-1042/digest decision
          → P-04 deep-object originatingRun filter
          → FILTERED_READY → owner result effect-77 only
390px mode → actual mount width 390 / one iframe
guide → 13 steps / collapsible / advanced viewport controls remain reachable
```

The actual inline transport core is unit-exercised across every material edge
profile. Wrong destination, widened coordinates, flat `originatingRun`, changed
digest, NEW with invented Agent ID and caller-selected Effect identity fail closed.

## A–O disposition

- A/B/C/E/F/H/I/K/O use validated owner-event transport;
- D/G/J/L remain owner-local `INTERNAL_ONLY` flows;
- M terminates at the exact child-owned `NO_DATA` boundary;
- N renders only the no-UI `FUTURE_PRODUCT_APP` disposition;
- O records the real P-01→P-04 boundary, then starts Brain governance separately.

## Gate

```text
current behavioral P11 = LOCKED / OPERATOR APPROVED
P12 findings against P11 = CORRECTED
P12/4C closure reconciliation = NEXT
```

Product/4A/4B, 4D, merge and Product implementation remain unauthorized.
