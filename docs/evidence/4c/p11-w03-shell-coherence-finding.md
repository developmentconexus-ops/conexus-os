# P11-W03-F01 — W-03 shell coherence finding

> **Status:** `CLOSED / W-03 SHELL RE-LOCKED / OPERATOR APPROVED`
> **Found by:** operator P11 walkthrough
> **Scope:** W-03 outer shell/composition only

## Evidence

The exact locked W-03 artifact (`7434c561ef0cfbc43c81ab8dd1f72b13cf032135`) uses a bespoke two-item sidebar:

```text
Conexus · Metal Nobre
→ People & access
→ Audit
```

It does not inherit the current locked GF-01 structure used by the other Workspace blocks:

```text
topbar
Workspace/Project breadcrumb-switcher
Account/session control
complete current-scope rail
narrow drawer/scrim
contextual Conexus seam where eligible
```

P11 faithfully mounted W-03 and therefore exposed the divergence; P11 did not create it.

## Root cause and classification

W-03's inner Product interaction is coherent, but its approved P8 predates the terminal shell composition. The mismatch is structural, not final visual styling, because it changes navigation hierarchy, context orientation, Account/session availability and responsive shell behavior.

```text
classification = material P11 block-coherence falsifier
smallest owner reopen = W-03 P8 outer shell
Product/wire change = 0
inner W-03 interaction reopen = 0 unless shell integration falsifies it
```

## Preserved W-03 truth

The correction must preserve without redesign:

- People/Areas human-subject tabs and local disclosed-list find;
- membership candidate discovery/pagination and Add member;
- exact person panel with IAM-19 DIRECT/AREA sources;
- exact Area panel and IAM-20 members/Project grants;
- IAM-05..12 writes followed by owner re-read;
- sibling Workspace Audit route;
- OBS-04 server filters before pagination;
- OBS-05 immutable record detail/Evidence;
- every loading/empty/denied/dependency/stale and focus/recovery obligation.

## Selected correction

Replace only W-03's bespoke outer `.app/.sidebar` composition with the current GF-01 Workspace shell. Route `People & access` and sibling `Audit` through the normal complete Workspace rail. Preserve W-03 content containers and scripts, adapting only shell navigation/drawer/context/account wiring needed for composition.

Required proof:

```text
GF-01 topbar/breadcrumb/Account/full rail present
People & access + Audit navigate in that shell
all prior W-03 inner interactions still operate
wide/narrow drawer + panels/dialogs remain usable
old bespoke two-item sidebar absent
Product/wire/Permission counts unchanged
operator re-lock required
```

The prior P11 candidate `be777c484d09e197aa95d61c6b2891304b1094cd` remains invalidated. W-03 is re-locked at `e9d630622d853d6e352737f46202f2765526fd6a`; a new guided P11 blob must mount this exact identity and be reverified.
