# 4C pre-P11 global coherence + assembly-readiness fusion review

> **Status:** LEAD ADJUDICATION / `BOUNDED CORRECTION BEFORE P11` / `F03-F05 OPERATOR APPROVED`
> **Reviewed state:** current local 4C worktree through the operator-locked `PA-01` artifact
> **Prior P11 candidate:** `REJECTED — WRONG STRUCTURE / INSUFFICIENT INTERACTION EVIDENCE`
> **Product implementation:** blocked

## 1. Review protocol and provenance

Common handoff: [`pre-p11-global-coherence-review-brief.md`](pre-p11-global-coherence-review-brief.md)

Independent reviewers:

```text
Claude Code CLI 2.1.220
model = claude-opus-5
effort = xhigh
role = Principal Product Architect / Engineering Method challenger

AGY CLI 1.1.22
model = gemini-3.1-pro-high
effort = high (maximum exposed by AGY)
role = Principal Product Designer / Information Architecture challenger

Codex Lead
role = repository-current authority reconstruction, independent falsification,
       external-reference disposition and Engineering Method adjudication
```

Both external reviewers ran in read-only planning/sandbox posture against the same brief. Gemini initially returned `READY FOR P11`; a directed second round required it to address the mechanical counterexamples below. It revised its verdict to `BOUNDED CORRECTION BEFORE P11`. Reviewer agreement is Evidence only; every finding below is independently adjudicated.

The current review target is not yet an immutable Git candidate. `HEAD = origin/main = 1619f14`; later locked P-01/P-02 revisions and P-03…PA-01 artifacts exist only in the current worktree/loose-object state. Exact current HTML hashes still match every Screen Contract's pinned blob identity.

## 2. Lead verdict

```text
METHOD FINDING                         = 0
PRODUCT / PLAN GAP                    = 3 material
LOCAL EXECUTION GAP                   = 6 material/important
DEFER SAFELY                          = 1 explicit seam
accepted 4A/4B authority survives     = YES, except bounded reopen candidates below
locked block semantics survive        = YES, except explicitly named bounded frontend deltas
READY FOR P11                         = NO
verdict                               = BOUNDED CORRECTION BEFORE P11
```

The package is directionally coherent and avoids speculative framework/runtime authority. It is not yet a complete terminal P10/P11 input because coverage proof, first-access work, two cross-block authority seams, global pattern reconciliation and immutable provenance remain unresolved.

## 3. Findings that survive Lead adjudication

### `4C-PRE11-F01` — LOCAL EXECUTION GAP / MATERIAL — no faithful-assembly contract or proof

**Evidence**

- Frontend Method P11 requires assembly of already-locked prototypes, complete journeys, deep links, shared overlays, negative/recovery and responsive behavior.
- The rejected P11 replaced protected block structures with new summary cards and still passed `7/7` because its test checks strings and JavaScript parsing only.
- The rejected plan said local detail was delegated, but the HTML neither embedded nor navigated to the locked P8 structures.

**Root cause**

The Conexus P11 preparation/proof strategy had no block-by-block preservation matrix. Names and handoff labels were treated as evidence that interaction structure survived.

**Adjudication**

This is not yet a reusable-method defect. The method already prohibits redesign and requires assembly. One local misexecution does not satisfy the Engineering Method threshold for changing an organizational method.

**Global Maximum**

Before new P11 HTML, create one falsifiable Conexus assembly contract:

```text
locked block
→ protected surface/region
→ protected controls/interactions
→ material owner-specific states
→ exact entry/exit coordinates
→ P11 destination
→ journey + executable proof
```

The proof must fail when any protected entry is absent or inert. Do not require iframes, a framework, a component system or byte-for-byte duplication.

**Must not reopen:** the twelve locked structures merely to make assembly easier.

### `4C-PRE11-F02` — LOCAL EXECUTION GAP / MATERIAL — coverage matrix and test are not bidirectional/current

**Evidence**

The current 4C-5 coverage rows omit accepted, frontend-reachable operations already realized by later locked blocks:

```text
PRJ-25..28  Project Data Explorer
BLD-18..20  Product Agent Change draft
BRN-13      Project Analytic Query catalog
BRN-14      Project Brain Context
```

`PAR-05` remains the sole valid direct-browser `NOT-HUMAN-FACING` disposition.

The current repository test proves only that mapped operations are valid; it does not prove that every current frontend-reachable operation is mapped or explicitly dispositioned. Historical `116/117` counts remain in the candidate foundation/inventory while current authority is `127↔127`.

**Global Maximum**

Boundedly recompile the current coverage rows and make the test prove both directions:

```text
mapped - admitted = 0
admitted frontend-reachable - mapped - explicit dispositions = 0
```

Do not add operations or restart 4A/4B.

### `4C-PRE11-F03` — PRODUCT / PLAN GAP / MATERIAL — Journey A trusted setup is not functionally realized

**Evidence**

The accepted inventory defines:

```text
CP-S01 Account/session frame → IAM-01/IAM-02
CP-S02 first access / Workspace chooser/create → WS-01
CP-S03 trusted Account provisioning → IAM-03
```

`IAM-03` and `WS-01` are current `HUMAN_ACCOUNT_SESSION / CP` Product commands for the trusted platform operator. No locked block Screen Contract/functional HTML owns CP-S02 or CP-S03. GF-01 traces `IAM-02 EndSession`, but its Account button is not operable in the canonical P8.

**Failure mode**

All named blocks may be assembled while the accepted first-access/Workspace-establishment journey remains impossible to operate.

**Global-Maximum candidates**

1. one bounded trusted-access/bootstrap block covering Workspace chooser/create and trusted Account provisioning, plus a bounded GF-01 Account/session interaction delta;
2. explicit operator disposition that one or both surfaces are outside the current platform frontend program, with a truthful alternative operational home.

Because the operations are currently classified as browser-human Product commands and the inventory already declares material route surfaces, silently treating them as non-frontend is not valid. Operator adjudication is required.

**Must not reopen:** public signup, invitation, Keycloak organization/role authority or the GF-01 shell hierarchy.

### `4C-PRE11-F04` — PRODUCT / PLAN GAP / MATERIAL — exact AgentRun → effect investigation has no admitted anchor

**Evidence**

P-03 promises:

```text
exact AgentRun
→ Continue to Activity
→ inspect effect attempts/reconciliation
```

The P11 composition plan carried `projectId + agentRunId`. However `OBS-01 ListProjectActivity` and `GW-01 ListEffectAttempts` currently accept pagination only; P-04 declares no inbound AgentRun filter/anchor. The destination can only open an unfiltered Project collection, and browser filtering of a loaded page cannot define the owner result set.

**Global-Maximum candidates**

1. narrow the P-03 wording/control to an honest unfiltered Project Activity browse;
2. preserve the accepted exact-investigation job and add a bounded server-owned originating-run filter to the existing Gateway effect-attempt read, with no new operation;
3. remove the continuation, suppressing the accepted investigation need.

Option 2 is the leading sustainable structure if exact continuation is confirmed as the intended human job. It preserves one owner, server-side filtering before pagination and exact route coordinates. Operator Product adjudication is required before a 4A/4B bounded recompile.

**Must not reopen:** Gateway retry/replay authority, Activity current-truth limits or the whole P-04 block.

### `4C-PRE11-F05` — PRODUCT / PLAN GAP / MATERIAL — build-only Agent creators lack governed reference discovery

**Evidence**

P-03/P-01 deliberately preserve independent permissions:

```text
project.build != project.read != project.source.read != agent.trigger.manage
```

The locked Agent Studio permits NEW/EXISTING typed definition work including capability bindings, Brain context and policy/approval/budget/verification references. Yet `PRJ-16/17` capability discovery remains under `project.read`, and no accepted human-recognizable discovery owner is identified for the remaining governed references when the creator holds only `project.build`.

**Failure mode**

A build-only creator can be authorized to author an Agent but cannot truthfully discover the human-recognizable governed subjects required to complete a safe NEW definition. Free-form IDs, fixture catalogs or a frontend-owned registry would patch around the missing authority.

**Global-Maximum requirement**

Run one bounded Product/authority decision before P11:

```text
prove exact creator jobs and minimum reference sets
→ identify each semantic owner
→ compare purpose-bound reuse, permission composition and owner-projected draft choices
→ reject screen-shaped aggregate catalogs and frontend mirrors
→ select the smallest human-recognizable read composition
```

No solution is ratified by this review. Operator Product adjudication is required.

### `4C-PRE11-F06` — LOCAL EXECUTION GAP / IMPORTANT — no terminal P10 pattern vocabulary

**Evidence**

W-03 graduates `context-preserving exact-subject panel`. Later P-03/P-04/P-05/PA-01 contracts describe repeated protected semantics—owner paging, stale/current refusal, loading/empty/denied/failure separation, focused overlays and focus return—but no final global reconciliation determines what is truly shared versus owner-specific.

**Global Maximum**

Produce one terminal semantic vocabulary before P11. It must describe protected behavior, consumers and exclusions, not components/hooks/stores. Keep PA-01's full-page/panel/inline host observation ungraduated until repetition supports it.

### `4C-PRE11-F07` — LOCAL EXECUTION GAP / MATERIAL — live status routes to a rejected P11

**Evidence**

The roadmap and index still describe the prior P11 as an assembled candidate with a possible `LOCK` decision. The operator has rejected its structure, and the whole-package audit confirms insufficient interaction Evidence.

**Global Maximum**

Keep the artifact as negative Evidence, but status it as:

```text
P11 prior candidate = REJECTED — WRONG STRUCTURE
P11                 = NOT ASSEMBLED
next                 = adjudicate/close pre-P11 findings
```

### `4C-PRE11-F08` — LOCAL EXECUTION GAP / MATERIAL — locked artifact provenance is not durable

**Evidence**

Several current locked HTMLs/Screen Contracts and later P-01/P-02 revisions are untracked or unreachable from any Git commit. GitHub `main` contains the earlier 4C checkpoint, not the current through-PA-01 package.

**Failure mode**

A fresh clone cannot independently reconstruct the exact package being reviewed. Pinned hashes prove current worktree integrity, not durable reachability.

**Global Maximum**

After accepted review corrections, create one coherent checkpoint commit on the current branch before the final independent whole-package review. Merge remains separately operator-gated. Do not stage or commit the current dirty worktree without explicit operator authority.

### `4C-PRE11-F09` — LOCAL EXECUTION GAP / IMPORTANT — shared shell terminology and fixture universe need terminal adjudication

**Evidence**

- inherited shells use `Settings`, while later P-05 locks a bounded `Manage` destination and rejects generic Settings;
- early locked fixtures use `Budget Analyzer`, later Project blocks use `Sales Operations`, and PA-01 uses `Revenue Desk`.

These are not Product identity contradictions, but an assembly can silently rewrite locked presentation if the normalization rule is not explicit.

**Global Maximum**

At terminal P10, record:

1. the operator-owned final navigation label (`Manage` is the current leading candidate);
2. which fixture names may be normalized for one continuous P11 journey;
3. that Budget-related Brain concepts, capability names and Project fixtures may remain visible without creating a Budget Analyzer application wireframe.

No block structure or Product semantics should reopen for fixture naming alone.

## 4. Explicit safe deferral

### `4C-PRE11-D01` — approval-owned richer originating-run context

The current approval owner already supplies the exact sealed proposal, immutable request-time Agent/action/time/Release recognition and originating `agentRunId`. A richer safe run projection for an approver lacking `project.read` may improve the decision, but the current Evidence does not yet prove it is required.

```text
disposition = DEFER SAFELY
probe       = real P11/P12 approval walkthrough
reopen      = only if a human cannot decide safely from current owner truth
forbidden   = unauthorized PAR-07 join or frontend-composed run context
```

## 5. Decisions that survive attack

The fusion review found no material reason to reopen:

- the Workspace → Project → Published Application authority hierarchy;
- Data / Brain / Capabilities / Integrations as distinct Project Product lenses;
- Connection lifecycle versus Project Integration use;
- Workspace Brain publication versus Project Brain context/binding;
- authored Product Agent versus Release versus runtime execution;
- Preview / verified / Release AVAILABLE / Promotion / pointer / `SERVED_VERIFIED` separation;
- Activity/Audit/Evidence as projections rather than current owner truth;
- exact contextual ApprovalRequest and rejection of a universal Approval Center;
- app-first Build with Preview dominant and Conexus on the right;
- context-preserving exact-subject panel as the one currently graduated pattern;
- PA-01 full-page, contextual-panel and inline hosts as three locked compositions over one owner truth;
- no final design-system, component, SDK, router, generator, state-library or runtime selection in 4C;
- no Budget Analyzer application wireframe in current platform scope;
- current `127↔127` Product/wire authority, except only the bounded read-shape questions explicitly named above.

## 6. External primary-reference dispositions

### Replit Project Editor

**Source observation:** the current Project Editor keeps conversation with Agent and live application Preview together as the builder's home base.

**Inference:** the locked P-01 app-first/right-Conexus structure remains globally competitive and human-task-centered.

**Conexus disposition:** `PRESENT-IN-AUTHORITY / NO FINDING`. Do not import Replit thread/task ontology or runtime authority.

### Microsoft Purview Unified Catalog workflows

**Source observation:** governance-domain publication keeps draft/review/approval/publication as explicit governed stages for catalog/glossary work.

**Inference:** W-02A's proposal decision and publication separation is not accidental complexity; it protects human governance.

**Conexus disposition:** `PRESENT-IN-AUTHORITY / NO FINDING`. Do not import a generic workflow center.

### Vercel deployment/promotion documentation

**Source observation:** immutable deployments, promotion, rollback/current target and actual serving assignment are distinct concepts; deployment checks may gate promotion.

**Inference:** P-04's Release/Promotion/pointer/serving distinctions are essential rather than overdesigned. Conexus's independent `SERVED_VERIFIED` truth is intentionally stricter.

**Conexus disposition:** `PRESENT-IN-AUTHORITY / NO FINDING`. Do not import Vercel environment defaults, domain mechanism or deployment ontology.

Additional external research stopped because it no longer changed the decision space.

## 7. Operator decisions

The review can apply the local corrections in `F01`, `F02`, `F06`, `F07` and `F09` without changing Product meaning. On 2026-08-27, the operator approved all three material decision directions:

1. `F03` — admit minimal secure one-shot trusted setup for the operator-owned Conexus instance, without SaaS/tenant onboarding machinery;
2. `F04` — preserve exact AgentRun→effect investigation through a bounded server-owned `originatingRun` filter on the existing Gateway read;
3. `F05` — `project.build` must include safe purpose-bound discovery of the contracts required to build without gaining generic data/secret/runtime/admin authority; execute the bounded per-reference-family study before selecting the exact wire realization.

The exact decision text and continuation limits live in [`pre-p11-operator-adjudication.md`](pre-p11-operator-adjudication.md).

Repository authority is also required before staging/committing the eventual coherent checkpoint described by `F08`.

## 8. Smallest sustainable continuation

```text
record prior P11 as rejected negative Evidence
→ operator adjudicates F03 / F04 / F05
→ apply only accepted bounded corrections
→ recompile coverage bidirectionally
→ terminal P10 vocabulary + shell/fixture adjudication
→ define executable faithful-assembly contract
→ required verification
→ immutable checkpoint + fresh independent review
→ only then P11 assembly
```

P12, 4D, merge and Product implementation remain blocked.
