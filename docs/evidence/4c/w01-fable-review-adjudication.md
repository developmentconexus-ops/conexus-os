# 4C W-01 — Fable Adversarial Review Adjudication

> **Status:** REVIEW ADJUDICATED / C1-R1 SURVIVES / OPERATOR GATE / NOT LOCKED
> **Reviewed candidate HEAD:** `89baef196b7ac9f567e645207b857de9adfb495a`
> **Review branch:** `review/4c-w01-fable` — temporary Evidence only / never merge
> **Scope:** W-01 C1-R1 Baseline visual review + `4C-F03` authority boundary + explicit non-generalization to future Plan visualization.
> **Authority posture:** reviewer output is Evidence. Lead adjudicates against current repository authority; only the operator may mark W-01 `LOCKED`.

The independent Fable review reproduced the current Linux-filesystem verification result at `54/54` and returned:

```text
VERDICT = SURVIVES
material findings = 0
final reviewer recommendation = LOCK candidate
```

The Lead does **not** inherit the reviewer recommendation as authority. This adjudication evaluates every review conclusion and routes only properties supported by current authority/Evidence.

---

## 1. Lead disposition

```text
C1-R1 architecture / platform-law direction = SURVIVES
4C-F03 Product correction                   = SURVIVES
PRJ-07 refinement boundary                  = KEEP
PRJ-24 candidate contextual explanation     = KEEP
ordinary Permissions                        = 25 / KEEP
Lavish disposition                          = ADAPT properties / KEEP
Mastra authority boundary                   = KEEP
Baseline-specific scope                     = KEEP
shared visual-review framework              = DEFER
Plan visual grammar                         = DEFER to real P-01 consumer
W-01 candidate revision before operator     = NOT REQUIRED
W-01 LOCKED                                 = NOT SET BY LEAD
```

No accepted review point requires reopening 4A, 4B, GF-01 or another 4C block.

---

## 2. Material attack routes R1–R9

### R1 — Platform law vs frontend authority — ACCEPT REVIEW CONCLUSION

The current candidate preserves the required authority separation:

```text
exact immutable candidate
→ server-derived/revalidated review subject
→ deterministic visual projection
→ generated candidate-local selection context
→ local conversation/refinement draft
→ explicit admitted Product operation
→ owner-produced new truth
```

`candidateBaselineDigest` may be carried in URL navigation for re-entry but browser/DOM/anchor/chat state does not become Baseline authority.

No correction required.

### R2 — Baseline remains Project-owned — ACCEPT REVIEW CONCLUSION

Current authority stays inside the Project owner:

```text
PRJ-07 investigation/refinement
PRJ-08 approved Baseline read
PRJ-09 exact candidate approval
PRJ-23 exact candidate read
PRJ-24 exact candidate contextual explanation
```

No `BaselineComment`, `BaselineThread`, `ReviewSession`, generic editor or candidate mutation owner is admitted.

No correction required.

### R3 — `project.manage` vs `project.build` — ACCEPT REVIEW CONCLUSION

The distinction remains materially useful:

```text
PRJ-24
subject = exact immutable candidate
human job = understand/adjudicate Baseline
Permission = project.manage

BLD-16
subject = Builder/Change Project context
human job = create/evolve accepted Project intent
Permission = project.build
```

Merging these now would either require Baseline reviewers to hold Builder authority or widen BLD-16 into a global assistant owner.

No correction required.

### R4 — Lavish disposition — ACCEPT REVIEW CONCLUSION

Current Global-Maximum disposition remains:

```text
ADOPT Lavish Product architecture directly = REJECT
BUILD full Conexus clone/editor now          = REJECT / YAGNI
ADAPT useful collaboration properties       = KEEP
```

Properties retained:

```text
portable/rich review projection
precise selected context
conversation beside artifact
feedback queue before action
re-projection after canonical change
```

Lavish file-path/session/local-server/polling assumptions remain non-authoritative references only.

No correction required.

### R5 — Mastra boundary — ACCEPT REVIEW CONCLUSION

Current 4C wire/P8 has no Mastra thread/session/memory authority.

Binding law remains:

```text
Mastra Agent        = cognition/mechanics
Mastra streaming    = possible interaction mechanism
Mastra thread       = possible continuity mechanism
RequestContext      = runtime context carriage

all of the above -X-> Product/Baseline authority
```

No correction required now. A 4D carry-forward is recorded in §5.

### R6 — Methodology alignment — ACCEPT REVIEW CONCLUSION

The current chain is materially falsifiable rather than ceremonial:

```text
4C-F03 clean RED
→ operator correction gate
→ bounded 4A/4B correction
→ GREEN

C1-R1 structural RED
→ revised bounded P8
→ GREEN
→ independent Fable challenge
→ Lead adjudication
→ operator gate
```

The correction added one Product operation, enriched one existing operation and added zero Permission/owner/durable-record classes.

No methodology correction required.

### R7 — Baseline review vs Plan visualization — ACCEPT / IMPORTANT NON-GENERALIZATION LAW

This is the most important architecture result of the review.

The Baseline consumer does **not** prove a reusable visual-review/rendering framework by itself.

Baseline and Plan differ materially:

```text
Baseline candidate
= immutable candidate under exact digest
= review/adjudication truth

Plan
= Hub-owned live planning/checklist truth
= WorkUnits / dependencies / assertions / blockers / progress
= current revision/state transitions may change during work
```

Therefore:

```text
Baseline C1-R1 -X-> universal ReviewProjection owner/framework
Baseline C1-R1 -X-> chosen Plan graph/canvas/DAG/timeline grammar
```

Plan visualization remains a separate real consumer to be derived when `P-01` opens. Only after a second real consumer proves repeated properties may 4C/4D graduate a shared primitive.

### R8 — Future Plan interaction premise — ACCEPT AS PLATFORM-LAW PREMISE ONLY

A future Plan surface may reuse the same authority law without inheriting Baseline rendering choices:

```text
Hub-owned exact current truth
→ projection
→ human selects context
→ contextual explanation/proposal
→ conversation does not mutate
→ explicit admitted operation crosses authority boundary
→ owner writes current truth
→ re-projection
```

This is a semantic/authority premise, not a visual grammar or reusable component decision.

### R9 — Global Maximum / YAGNI — ACCEPT REVIEW CONCLUSION

Current surviving combination is:

```text
Lavish collaboration properties
inside
Baseline-specific minimum scope
```

Rejected now:

```text
direct Lavish Product architecture
generic visual-artifact owner
universal review framework
shared Plan/Baseline renderer before second consumer
```

No correction required.

---

## 3. MIN-01 — candidate digest recoverability

Fable correctly identified a bounded usability limitation:

```text
PRJ-07 returns exact candidate digest
URL may carry it for re-entry
PRJ-23 can read exact candidate by digest
BUT
current Project representation does not discover an already-existing unapproved candidate digest
```

Lead disposition:

```text
ACCEPT AS NON-BLOCKING DEFERRED CONSUMER QUESTION
```

Why not correct now:

- no authority violation;
- no wrong-candidate approval path;
- current accepted W-01 Journey-B consumer is continuous review/re-entry from the known exact candidate;
- adding a current-candidate/list/discovery operation without a proved multi-reviewer/new-device/handoff consumer would violate YAGNI.

Reopen trigger:

```text
real human workflow requires discovering/recovering an existing unapproved candidate without possessing its digest
```

Smallest owner then = Project.

---

## 4. MIN-02 — projection version / failed anchor revalidation

Lead disposition:

```text
ACCEPT AS 4D REALIZATION CARRY-FORWARD / NO 4B WIRE CHANGE NOW
```

Current 4C only needs the protected property:

```text
review context is generated/untrusted
server revalidates it against the exact immutable candidate
```

4D must choose the concrete realization and close:

```text
server-side review-projection version identity
exact deterministic anchor derivation/re-resolution
explicit mismatch behavior when selected context cannot be re-resolved
```

A client-supplied projection version is not automatically required; it must be admitted only if the 4D mechanism needs it.

---

## 5. MIN-03 — non-Linux repository-proof sensitivity

Fable observed CRLF/case-insensitive-filesystem failures outside the repository-mandated Linux-filesystem environment.

Lead disposition:

```text
VALID ENGINEERING OBSERVATION
NOT A 4C / PRODUCT FINDING
NO CURRENT CANDIDATE CORRECTION
```

Current repository authority already mandates the supported WSL2/Linux-filesystem worktree used by qualification/verification, and the reviewer reproduced `54/54` there.

Potential hardening (`*.html`/`*.mjs` LF pinning and case-exact superseded-path checks) belongs to repository engineering/verification-kit maintenance if cross-filesystem support becomes a real requirement. It must not reopen W-01.

---

## 6. Watch items retained

### WATCH-01 — contextual explanation repetition

Current two operations do not prove a shared Product operation/owner:

```text
BLD-16 AskConexusAboutContext
PRJ-24 AskConexusAboutBaselineCandidate
```

Trigger for re-evaluation:

```text
a third materially similar owner-specific contextual-explanation consumer
OR
P-01 proves a repeated exact contract that reduces rather than obscures owner semantics
```

Until then, do not create a universal assistant operation.

### WATCH-02 — Mastra conversation continuity

When 4D realizes PRJ-24:

```text
Mastra thread/run/memory identity
-X-> deciding Product-wire field
-X-> Baseline truth
-X-> authorization
```

Current authority must always be re-resolved by Conexus owners/tools.

### WATCH-03 — refinement structure quality

Current `reviewFeedback` may remain a bounded human feedback carrier.

If real quality Evidence later shows that flattening feedback loses material selected-section meaning, the smallest correction is richer explicit **input context**, not promotion of generated anchor/DOM identity into Product authority.

---

## 7. Plan visualization timing

The repository already accepts that a Visual Plan may expose:

```text
WorkUnits / work items
dependency graph
acceptance/assertion links
known blockers/unknowns
current progress
```

What remains intentionally undecided:

```text
DAG vs cards vs outline vs timeline vs canvas
Mermaid or other diagram mechanism
single vs composed views
interaction density
layout grammar
shared renderer/component architecture
```

Exact decision route:

```text
P-01 real Plan/Builder human job
→ current Plan/Builder authority
→ reference study including Lavish where relevant
→ competing structural hypotheses
→ authority feasibility
→ lo-fi HTML Evidence
→ independent challenge when material
→ operator decision
→ 4D mechanism realization
```

Do not import C1-R1 Baseline sections into Plan by analogy.

---

## 8. Final Lead adjudication

```text
Fable verdict                  = SURVIVES
Lead material corrections      = 0
4A reopen                      = NO
4B reopen                      = NO
GF-01 reopen                   = NO
W-01 candidate revision        = NO
Plan visual grammar selected   = NO
shared review framework        = NO
operator lock gate             = READY
```

Current candidate remains:

```text
W-01 C1-R1 = HTML CANDIDATE / FABLE SURVIVES / NOT LOCKED
```

The exact next action is operator visual/interactive adjudication:

```text
LOCKED | REVISE | REJECTED
```
