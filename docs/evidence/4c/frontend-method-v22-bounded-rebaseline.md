# 4C — Frontend Method v2.2 Bounded Rebaseline

> **Status:** OPERATOR APPROVED / GREEN
> **Method:** Frontend Product Experience Planning Method v2.2
> **Scope:** methodology/program mapping only; no Product operation, owner, Permission or wire authority created.

## Decision

The canonical frontend planning method was replaced by v2.2 and the Conexus 4C profile was boundedly recompiled rather than restarted.

```text
method v2.2
→ P8 functional low-fidelity HTML for material interactive blocks
→ operator operates/revises/LOCKs each block
→ P9 exact screen/backend trace
→ P10 derived patterns
→ P11 assembled interactive low-fi Product
→ P12 whole-product adversarial walkthrough
```

## Bounded rebaseline result

```text
GF-01 H1-R2 = PRESERVED LOCKED
W-01 C1-R1 = PRESERVED LOCKED
```

No v2.2 property currently falsifies either lock. Both already use browser-operable HTML for their material interaction/structure. Later P11/P12 Evidence may still reopen the smallest block if a whole-product contradiction appears.

## Conexus Phase-4 mapping

```text
FP0 / P0–P5  → 4C foundation
FP1 / P6–P10 → 4C block-by-block functional experience
FP2 / P11    → 4C assembled interactive low-fi Product
FP3 / P12    → 4C whole-product UX/architecture challenge
FP4 / P13–P14→ carried across the broader Phase-4 readiness program
```

4C produces visual-design handoff inputs and structural conformance obligations. 4D–4G remain mandatory for Conexus-wide implementation readiness; v2.2 adoption does not bypass them.

## Protected law

```text
P8 proves the block; P11 proves the product
```

For every P7 leading hypothesis, required fields/summaries, identity, scale, filter/sort, preview/content truth and material writes must be `PRESENT-IN-AUTHORITY` or a `FINDING` before P8 LOCK.

## TDD

```text
Verify #579 = expected RED
→ 63 tests / 62 pass / 1 fail
→ only missing v2.2 adoption in the Conexus profile

Verify #580 = expected intermediate failure
→ v2.2 adoption passed
→ existing GF-01 guard correctly caught loss of the canonical P8 medium wording

Verify #581 = GREEN
→ v2.2 profile + canonical P8 medium + prior locks/gates preserved
```

No Product implementation or merge authority is implied.
