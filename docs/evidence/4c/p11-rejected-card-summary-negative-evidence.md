# P11 rejected card-summary candidate — negative Evidence

> **Status:** `REJECTED — WRONG STRUCTURE / INSUFFICIENT INTERACTION EVIDENCE`
> **Disposition:** superseded by faithful exact-artifact assembly; never a locked baseline

This record preserves the deciding negative Evidence from the first P11 candidate after its canonical HTML path was re-authored for the new candidate. The original candidate was not accepted Product authority and must not be reconstructed or used as a baseline.

## Observed rejected realization

```text
generated Control Plane shell
→ route labels and summary cards
→ generic dialogs/global scenarios
→ simplified Published-App sample
```

It claimed `12 locked blocks composed`, but:

- omitted T-01 entirely;
- reduced GF-01 to an incomplete shell without the locked Account/session and full switcher behavior;
- represented W-01..W-04 and P-01..P-05 mostly as cards, labels and generic dialogs;
- omitted material P-01 Build/Agent Studio, P-02 Product lenses, P-03 Agent operations, P-04 owner lenses, P-05 access/lifecycle and PA-01 Conversation/decision behavior;
- used global state scenarios instead of owner-specific states and recovery;
- left material tabs/buttons/boundaries inert;
- did not prove exact cross-block coordinates, focus restoration or responsive composition;
- treated lexical presence as assembly coverage.

The pre-P11 fusion review and later 13-block audit independently confirmed the same root cause:

```text
block names present != locked interaction structure preserved
summary screen != assembled Product
static lexical test != behavioral P11 proof
```

## Surviving obligations

The rejected candidate contributed only falsifiers carried into current authority:

1. P11 must use all 13 locked inputs including T-01;
2. exact local controls/states must remain operable;
3. adapters must carry only owner-issued coordinates and re-resolve destination truth;
4. one active Control Plane shell and independent Published-App shell must remain distinct;
5. owner-specific negative states cannot become a global scenario switch;
6. inert material controls must equal zero;
7. Budget Analyzer application UI remains absent;
8. P11 must not select 4D technology or begin implementation.

Current positive authority lives in `p11-faithful-assembly-contract.md`, the rebaselined composition plan and the new canonical P11 candidate. This negative record cannot lock or reopen Product meaning by itself.
