# R2 P4/P5 production composition — independent closure review brief

> **Repository:** `/home/leandrotheodoro/conexus-os`
> **Branch:** `codex/post-r1-session`
> **Code candidate:** `b8ec320`
> **Material range:** `3db3059..b8ec320`
> **Review mode:** fresh, isolated, read-only whole-package challenge

## Current gate

R2-P0..P3 are closed. P4 is open only for closure of the Project Brain-binding
production path. Earlier accepted P4 work owns authorization, Git/DB settlement,
recovery, removal, context and subject resolution. This candidate supplies the
previously missing P5-owned physical key producer and composes the complete
production path. It does not open R3+, RB/Mastra or later Budget calculations.

Start from repository authority rather than this summary:

1. `AGENTS.md`;
2. `docs/roadmap.md`;
3. `docs/index.md`;
4. `docs/evidence/4f/4f-r2-stage-code-packet.md`;
5. `docs/evidence/4f/4f-r2-p5-sankhya-source-admission-gap.md`;
6. only the Product, architecture, contract, code and proof owners needed to
   decide a concrete question.

## Candidate learning and protected claims

Attack whether the candidate really makes PRJ-10/11/12 and BRN-14 reachable
without weakening accepted authority:

- the physical producer is exactly the fixed `TGFCAB`/`CODEMP`/
  `CODTIPOPER IN (14,714)`/`NUNOTA` aggregate and accepts no caller SQL, URL,
  credential, company or proof result;
- only trusted catalog entries for the immutable mapping and company `1|2`
  can create observers; malformed, duplicate, empty, writable or symlinked
  catalog state fails closed;
- the observed subject binds exact Project, Workspace, Connection revision,
  qualification, credential generation, environment, company-derived source
  scope, source revision and input digest, and drift cannot become PASS;
- Connections owns credential materialization/authentication, Gateway owns
  observation/admission, Project owns source/binding settlement and Brain owns
  validation; the import law is not bypassed;
- one encrypted credential backend and one source snapshot are shared, while
  settlement, attestation and subject resolution retain distinct restricted
  PostgreSQL roles;
- incomplete full-mode configuration activates no partial binding path; valid
  R1-only and Connection-only configurations remain valid;
- the full and Connection-only binding modules are mutually exclusive, routes
  are registered once and all owned resources close once;
- no provider response, fixture, transaction ID or test result can manufacture
  semantic authority or widen this proof into later Budget mapping.

A material falsifier is a reproducible false PASS, unauthorized provider or
repository effect, cross-scope credential/data disclosure, partial route
activation, privilege collapse, broken existing R1/R2 configuration, divergent
Git/DB settlement, or a correctness-critical authority missing from the current
owners. Style, historical wording or optional test strengthening is not by
itself blocking.

## Proof reconstruction

At minimum inspect the material range and run:

```bash
npm run r2:p5:check
npm run r2:p4:check
npm run r1:s2:import-law
git diff 3db3059..b8ec320 --check
```

The Lead additionally ran the gated provider-free real composition against an
isolated PostgreSQL 17 database and the admitted OCI Git image:

```text
R2-P5 configured P4/P5 composition settles PRJ-11 through local Sankhya stand-in
PASS 1/1 in 239.8 s
```

The compiled observer also passed the separately authorized real read-only
Sankhya checks for companies `1` and `2`. Do not repeat live Sankhya calls; that
grant was proof-specific and no secret or business row belongs in review output.

## Output contract

Return:

1. `VERDICT = PASS | REVISE | HOLD`;
2. blocker census and protected-claim census;
3. each finding classified as `METHOD FINDING`, `PRODUCT / PLAN GAP`,
   `LOCAL EXECUTION GAP` or `NO FINDING`;
4. exact file/line evidence, reproducible failure mode, materiality and smallest
   owner/correction;
5. CLI/model/session or conversation identity available to the lane.

Do not edit files, call providers, expose credentials, push, open a PR, merge,
deploy or invent new Product requirements. Reviewer output is Evidence; Lead
adjudication remains authoritative for this checkpoint.
