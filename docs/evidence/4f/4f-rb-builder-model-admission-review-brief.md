# RB Builder model admission — independent correction challenge

## Subject and authority

Repository: `https://github.com/developmentconexus-ops/conexus-os.git`.
Exact correction candidate: `8de4855a58f4319e0a54c6b684de00482daa5f56` on
`rb-builder-first-vertical`.
Comparison base: `a26c75a01dcf8d311b491ca35e12a689c86c0362`.
This brief and later adjudication-only commits are not part of the correction
subject. This is a read-only material trust/authority-boundary challenge, not
merge, deployment, live provider execution or Change acceptance.

Reconstruct `AGENTS.md`, current roadmap/index, applicable Engineering and
Repository methods, Blueprint Harness §§10.4–10.6, the RB first-vertical stage
packet, and the smallest accepted Project model-admission owners cited there.
Repository-current authority supersedes this orientation. R1/R2 and the rest of
the already-reviewed RB candidate must not be casually reopened.

## Why this correction exists

Operator questioning exposed that the published Builder composition reused the
R1 purpose-specific `project-inception-opus-5` constructor. The correction is
intended to restore the accepted closed deployment-catalog portability law while
keeping Project model admission authoritative and Builder runtime model-neutral.

## Protected claims and falsifiers

- Builder resolves one exact, enabled, purpose-bound `BUILDER_CODING` admission
  from a finite server-owned catalog; no caller or guest selects provider/model.
- Unknown, disabled, duplicate, malformed, wildcard/latest, wrong-capability,
  unsupported-provider and missing-credential-slot configurations fail closed.
- The selected credential remains an external Project-owned credential-file
  reference. Secret bytes do not enter the catalog, ActorRun or guest material.
- Anthropic is only the currently implemented transport, not a Builder-domain
  semantic or a permanently fixed model. The exact catalog-selected model ID is
  passed to the provider adapter.
- Exact non-secret admission/provider/model identity is captured atomically when
  the authoritative ActorRun is admitted and cannot be supplied by worker output.
- The runtime refuses disagreement between the admitted model identity and the
  instantiated Mastra model. Mastra/provider objects do not become domain state.
- The existing R1 `project-inception-opus-5` admission remains unchanged for its
  original purpose. PRJ-29 remains Product-Agent policy discovery, not a raw
  Builder worker model picker.
- Migration ledger, least-privilege roles, serial writer, late-output refusal and
  existing R1/R2/RB behavior remain intact.

A reproducible violation, false-PASS route, secret exposure, owner contradiction
or inability to select another exact Anthropic coding model is material. Provider
breadth, UI model selection, automatic routing, ACP, eval tournaments and live
credential qualification are explicit non-goals unless the correction made a
current protected claim depend on them.

## Proof reconstruction and explicit unknowns

The Lead ran Linux typecheck, the targeted Builder/model admission tests,
`npm run rb:first:check`, the real PostgreSQL 17 RB proof, migration digest
verification, `git diff --check`, and full `npm run verify:local`; all passed.
The catalog test selects exact `claude-sonnet-4-5`, demonstrating the Builder
path is not fixed to Opus, and exercises the listed refusal classes. PostgreSQL
asserts exact identity persistence.

No live provider or E2B call is claimed because credentials/template admission
are not yet supplied. Do not invoke providers, start containers, edit files,
publish branches or perform external writes from this review. Determine whether
that is a bounded qualification gap or a concrete false-PASS in this correction.

## Output and limits

Use Blueprint §10.6 classes: `METHOD FINDING`, `PRODUCT / PLAN GAP`, `LOCAL
EXECUTION GAP`, or `NO FINDING`. Every finding must state exact evidence,
reproduction/failure path, materiality, smallest owner, protected invariant,
stop scope, required correction and what must not reopen. Distinguish an
unqualified live external property from a code defect. Review independently;
do not consult another lane. The Lead adjudicates findings. Reviewers cannot
accept RB, merge, deploy or expand the authorized slice.
