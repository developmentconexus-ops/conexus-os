# R3 RF-05/RF-08 operator admission waiver

> **Status:** OPERATOR APPROVED / INDEPENDENT REVIEW WAIVED / R3 IMPLEMENTATION ADMITTED
> **Date:** 2026-09-09
> **Authority:** explicit operator instruction in the active session

The operator explicitly approved the frozen RF-05/RF-08 candidate for R3
implementation and waived the remaining fresh Opus/Gemini two-lane review
because the prior review cycle had become excessive. This is a direct operator
admission; it is not independent closure, reviewer convergence, or a claim that
the waived lanes returned a PASS.

The quick GPT Sol low check remains advisory Evidence only. It confirmed the
candidate's mechanical consistency and identified the custody corrections that
were applied before this waiver: the transaction-source census is now derived
by the freeze checker, `project/inception.ts` is bound, the review/proof graph
scripts are in custody, the migration-lineage owner is explicit, and rollback
masking remains a named red R3 falsifier.

## Scope admitted

The implementation may now proceed within the accepted RF-05/RF-08 owner
decision and candidate boundary:

- Project-owned migration `024` and the hardened Project projection of the
  pinned pg-boss DDL;
- RF-05 transaction/access enforcement and its controlled negative fixtures;
- MAR occurrence, co-admission, recovery and read-only proof fixtures;
- exact local dependency and qualification work required by those slices.

The implementation must preserve the Project/MAR owner split, the R3/R7 proof
boundary, the BLD-10 `subjectDigest` non-consumption gate, and the 46-record
semantic inventory. Raw vendor SQL must not be applied verbatim.

This waiver does not authorize a real JobRun, live Sankhya/source access,
provider/model execution, deployment, Git publication, push, PR or merge. Those
remain separate operator actions. No R3 implementation result may be described
as independently reviewed until a later explicit review is run.

## Reopen and stop conditions

Stop and route the smallest owner if implementation evidence falsifies an
accepted owner, trust boundary, migration lineage, dependency pin, transaction
law, R3/R7 boundary or BLD-10 subject boundary. Reopen the waiver only for a
material contradiction or if the operator requests independent closure later.
