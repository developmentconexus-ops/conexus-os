# RB Builder shared model catalog — independent confirmation

## Frozen subject

Repository: `https://github.com/developmentconexus-ops/conexus-os.git`.
Exact corrected candidate: `ad79b6780501bd90ea0f9f44e5e58eaf0414e1c9`.
Correction comparison base: `8de4855a58f4319e0a54c6b684de00482daa5f56`.
Original RB comparison base remains `89c47b6f14b9e08146de378ba31784a97b9ea0f3`.
This brief is not part of the frozen code subject. Review is read-only and does
not authorize merge, deployment, credentials, providers, E2B or Change acceptance.

Reconstruct current repository authority from `AGENTS.md`, roadmap/index,
applicable Engineering/Repository methods, Blueprint Harness §§10.4–10.6, the
RB stage packet, the accepted 4E R1 ProjectMastra admission candidate §§4–5,
and the model-admission adjudication. Repository-current authority wins. Do not
consult another review lane or reopen unrelated accepted R1/R2/RB properties.

## Properties to attack

- One Project-owned closed catalog and one parser serve current R1 and Builder
  consumers. There is no second Builder catalog file, parser or semantic owner.
- Entries carry exact `providerKey`, `modelId`, `officialHttpsOrigin`, external
  `credentialSlot`, closed `capabilitySet`, `enabled` and unique `admissionId`.
- R1 still selects only exact `project-inception-opus-5` / Anthropic / Opus and
  requires its Inception plus Explanation capabilities even when Builder entries
  coexist. Builder selects its configured opaque ID and requires Builder coding.
  Cross-purpose use, missing capabilities and mutable aliases fail closed.
- Admission validates the selected OAuth file's existence, restrictive custody
  and structure before Builder ActorRun/sandbox creation, without refresh or an
  external call. Token use revalidates the same properties.
- Exact selected model identity remains inseparable from the model object in
  composition, is persisted atomically on ActorRun admission, and a firing test
  refuses model-ID disagreement at runtime construction.
- Caller/guest cannot select model/provider, secrets remain outside catalog,
  ActorRun and guest, and no model/provider object becomes domain authority.
- No provider breadth, Product picker, automatic routing/fallback, PRJ-29
  widening or legacy runtime abstraction is introduced.

A reproducible false-PASS, purpose crossover, R1 model substitution, secret
exposure, duplicate admission path, owner contradiction or startup-after-sandbox
credential failure is material. Preference for broader provider support or live
external qualification is not.

## Proof reconstruction

Lead reports GREEN: Hub typecheck; Biome over affected source/tests; R1 S6-P2
and production cognition boundaries; `npm run rb:first:check`; focused catalog,
credential-custody and runtime-disagreement controls; `git diff --check`.
No live credential, provider or E2B claim exists. Review source and cheap local
proof only; do not invoke external systems, start containers, edit or publish.

## Output contract

Use `METHOD FINDING`, `PRODUCT / PLAN GAP`, `LOCAL EXECUTION GAP`, or `NO
FINDING`. Every finding needs exact evidence, reproduction/failure mode,
materiality, smallest owner, stop scope, required correction and explicit
non-reopen scope. End with a compact verdict on the properties above. The Lead
adjudicates; reviewers cannot accept RB or merge.
