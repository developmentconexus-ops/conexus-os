# 4F(R1) — Corrected G0–S6 whole-implementation final confirmation brief

> **Mode:** two fresh isolated read-only lanes over one corrected frozen candidate
> **Candidate:** `docs/evidence/4f/4f-r1-complete-implementation-review-candidate-v2.json`
> **State:** `CANDIDATE / FINAL CONFIRMATION PENDING`
> **Claude substitution:** operator explicitly authorized model alias `opus` for this round because Fable quota is exhausted; do not report this lane as Fable
> **Authority:** reviewer output is Evidence; Lead adjudication owns closure

## Exact confirmation question

Determine whether the material defects found in the initial whole-R1 review are
corrected on production bytes and deciding proof without widening the frozen
nine-claim closure census. Attack the complete current G0–S6 implementation,
but concentrate effort on whether any correction is false, partial or creates a
new failure of the same protected claims.

The first Claude/Fable lane returned `REVISE`; the initial AGY attempt produced
no review because its headless permission allowlist denied inspection. Do not
borrow that verdict or infer convergence. This is the required fresh round
because the surviving corrections materially changed production composition
proof, cognition-boundary proof, database identity and review reliability.

## Authority and routed subject

Bootstrap from `AGENTS.md`, `docs/roadmap.md`, `docs/index.md`, Engineering
Method 1.2, Repository Method, Blueprint Harness 10.4–10.6 and the
`conexus-development` review routing. Then inspect:

- the v2 candidate manifest and initial review brief/manifest;
- `apps/hub/src/server.ts`, `http/app.ts` and the Project cognition/module files;
- S4/S6 migrations plus `apps/hub/migrations/atlas.sum`;
- S5 boundary and new S6 production-cognition/composed-journey tests;
- `.github/workflows/verify.yml`, `package.json`, import-law and review-wrapper tests;
- exact R1C-13 and native R1C-14 retained owners only as needed to challenge a
  protected claim.

Accepted Product meaning remains authority. Reopen only the smallest owner
actually falsified by current implementation Evidence.

## Frozen protected claims and blocker census

The blocking census remains exactly:

1. exact 13-operation tranche;
2. server-derived authority isolation;
3. persistence, migration, concurrency and re-entry correctness;
4. immutable Linux-native Git/OCI custody and false-PASS resistance;
5. cognition/OAuth safety and secret non-disclosure;
6. browser/HTTP/deep-link integrity;
7. exact Candidate/Baseline lifecycle;
8. proof integrity with controls shown to fire on the claimed subject class;
9. no unauthorized Product/provider/production/Git effect and preservation of
   existing/unowned state.

Framework recovery improvements, optional hardening and remote CI execution are
not blockers unless a reproducible observation shows that they falsify one of
these claims or make deciding proof unreliable.

## Corrections and falsifiers to challenge

- The composed test must actually load production Fastify, Project module,
  persistence and Mastra code, use real Chromium and PostgreSQL `17.10`, and
  traverse inception → Candidate A → explanation → Candidate B → approval →
  reload → stale-A conflict. Its model is explicitly a deterministic stand-in;
  it does **not** prove a live provider/model call.
- Production cognition proof must import shipped bounded fetch, Anthropic OAuth
  adapter and Project budget/cognition code and demonstrate egress denial,
  streamed-size/redirect cancellation, API-key stripping, bearer insertion,
  tool budgets, provenance, sanitized errors and zero retry.
- `MASTRA_TELEMETRY_DISABLED=1` must be set before the production import path
  can load Mastra-sensitive modules.
- PostgreSQL proof must refuse 16.10 and pass only
  `server_version_num=170010`; the workflow must use the exact admitted index.
- Atlas 1.3.0 exact bytes must reproduce the current migration hash through
  010. No reviewer should require regeneration merely because the prior brief
  called it stale; the deciding question is reproducibility.
- The Hub must cold-refresh `/projects/:projectId/inception` through the SPA
  fallback while API routes remain server-owned.
- Native R1C-14 historical evidence must retain its original Opus argv. New
  reviews default to Fable; this round alone uses the explicit Opus fallback.

## Current deciding proof

The v2 manifest records WSL Ubuntu proof after `npm ci`: `npm run verify` PASS;
R1C-13 isolated 23/23 PASS without a provider call; native R1C-14 31/31 PASS;
S6 P0–P5 PASS; S4 and S6 PostgreSQL tests PASS on 17.10; and production
composition/cognition 2/2 PASS. Reviewers may run only already-installed,
read-only targeted diagnostics. They must not install, mutate, access OAuth
credentials, call a Product/provider, or perform Docker/Git effects.

## Output contract

Return one self-contained report ending in JSON with `verdict` (`PASS`,
`REVISE`, or `STOP`), results for claims 1–9, concrete findings classified as
`METHOD FINDING`, `PRODUCT / PLAN GAP`, `LOCAL EXECUTION GAP`, or `NO FINDING`,
and `unresolvedMaterialFindings`. For every material finding provide exact
evidence, failure mode, smallest owner, protected claim, stop disposition and
scope that must not reopen. Keep unknowns explicit.

Do not compare lanes, ask another reviewer or claim consensus. A valid
non-blocker should be reported but must not silently expand this gate. A further
round is forbidden unless Lead adjudication finds a surviving material
correction that again invalidates a protected property or deciding proof.
