# P4 recovery and source ownership — implementation challenge

## Subject and authority

Repository: `https://github.com/developmentconexus-ops/conexus-os.git`.
Exact implementation candidate: `1c8dba3ff7e49ffb9430a05ef78fcdb615f1fb51`.
Implementation comparison base: `1f81a228678b803ab0a1946f6b0162e1815a1c7b`.
Subsequent brief/status-only commits are not additional implementation.
This is a material implementation challenge, not P4 closure or deployment approval.

Reconstruct AGENTS, roadmap, index, applicable engineering/repository methods
and Blueprint Harness §§10.4–10.6. The smallest task owner is
`4f-r2-stage-code-packet.md`, particularly the recovery/ownership findings and
§§A/B/E. Follow its exact Project/Permission owners only on a named uncertainty.
The operator authorized this implementation on 2026-09-05. Brain/Gateway
conformance producers, P5+, production and provider/model execution are excluded.

## Protected claims and falsifiers

- Durable Project intent precedes ref writes and freezes the original actor,
  declaration, source and exact apply/cancellation objects. Unknown transaction
  outcomes cannot be interpreted as semantic refusal or falsely return success.
- Current semantic admission is revalidated before apply and settlement. A
  changed qualification, credential or Permission cannot silently bind a new
  basis. Cancellation preserves the complete prior tree and binding rows through
  append-only CAS; no reset, prune or unowned-object deletion is allowed.
- Competing/restarted workers cannot settle conflicting terminal states or
  mutate an unknown source head. Only exact frozen objects can advance refs.
- Generated ownership is actually composed into the R1 source consumer; missing,
  contradictory or malformed classification cannot be disguised by fixture maps.
  Pending or raced source is refused before cognition. Historical seed/manifests
  and runtime role boundaries remain intact.
- Migration 012 preserves historical migration bytes and owner isolation, grants
  no direct runtime table access, and removes the reachable legacy settle bypass.
- Proof labels match their actual subjects. SQL fixtures do not prove Git/DB
  recovery, and local mocks do not prove process restart or live providers.

Challenge the complete diff, including module/server wiring and deciding tests.
Review ceremony or preferences block only if they expose a concrete false PASS,
false STOP, unauthorized effect or protected-property failure above.

## Reconstructed proof and explicit unknowns

Lead ran pinned WSL Node 24.20.0/npm 12.0.2: `npm ci`, `npm run verify`, Hub/web
typechecks, P4 local checks, Inception seven checks and native R1C-14 31 checks.
Real PostgreSQL: migration+P1 eight checks and P4 backfill/settlement two checks
passed. The latter use fixture source OIDs and make no Git claim. Disposable
PostgreSQL was cleaned up after execution; no real provider was called.

The new live OCI and composed Git/PostgreSQL tests are **not deciding PASS**:
source seeding stalled; a minimal exact-image `node --version` invocation also
timed out after 45 seconds. Inspect test strength, but do not manufacture runtime
proof or start containers from this read-only review. The extended repository
check separately refuses the unchanged pre-existing session-handoff path.
AGY's previously observed headless read-only command permission failure remains
an external review dependency, not a favorable verdict.

## Output and limits

Use Blueprint §10.6 finding classes; each surviving finding needs concrete
evidence, failure path, materiality, smallest owner, stop scope and required
correction. Distinguish unproven properties from reproducible defects. Lead
adjudicates; no reviewer can close P4. Raw output is retained outside the checkout
by `conexus:review`; durable adjudication routes through the existing stage packet.
No edits, installs, Docker mutations, provider calls, external writes, PR or merge.
Keep lanes isolated; do not consult another reviewer's output.
