# P4 permission increment and continuation-boundary challenge

Review subject: commit `9c99384` relative to `d2f18f9`, including all nine
changed paths. This is a partial P4 increment, not P4/stage closure. The Lead
requests one fresh isolated Fable and AGY round under the development method.

Bootstrap through AGENTS, roadmap, index and the applicable methods. Then read
the R2 stage code packet's P4 authority decision, mutation ceiling and
implementation finding. Accepted Product/Brain/Project meaning remains owning
authority; the Lead's finding and proposed successor are hypotheses to attack.

Protected claims / falsifiers:

1. Two independently stored and revocable Project creator facts follow the
   approved grant. Wrong account/Workspace/Project, generic grant inference,
   replay restoring revocation, or added disclosure/runtime authority falsifies
   this increment. Inspect migration, catalog verifier and real PostgreSQL test.
2. Source/checksum/schema custody and R1/R2 preservation survive the exact diff.
   No binding route or conformance implementation is claimed here.
3. The continuation decision stops only an actually underdetermined path.
   Challenge BOTH over-stopping and under-stopping: is the alleged Brain
   conformance/applicability gap already mechanically resolvable from accepted
   authority, or does implementing PRJ-11/BRN-14 require choosing missing
   semantic truth? Cite the smallest exact owner and concrete counterexample.
   Identify independently executable work if the stop is too broad. Do not
   pre-author new Product requirements or assume the Lead's proposal is needed.

Smallest additional route for claim 3: Brain reference §20.2; operation ledger
§5.5.3–5.5.5 and BRN-14; canonical project-brain-context and project path schemas;
`bootstrap-r2-brain.mjs`, Brain store and migration binding-validation shape.
Expand only to resolve a named material question.

Proof reported by Lead: npm ci; real PostgreSQL P4 and P1/P2/P3 regression;
R1 S4/S6 database/composed-browser/RC-01 checks; native Git custody; relevant
type/import/Biome and S6 browser checks. Synthetic model/HTTP fixtures prove
only local contracts, never real providers. No live provider proof is claimed.
Inherited failures: hygiene rejects the unchanged handoff filename, and S2's
read test expects an obsolete server literal. Full repository test is 319/320
because of that hygiene failure. This is not publication readiness.

Read-only review; no dependencies, code edits, Docker or Product/provider calls,
Git mutations or other reviewer. Reconstruct with source inspection; do not
repeat long suites. Return concrete METHOD FINDING / PRODUCT / PLAN GAP /
LOCAL EXECUTION GAP / NO FINDING classifications, evidence and failure mode,
materiality, smallest owner, stop/continue disposition and preserved boundaries.
Return only your independent verdict, not consensus. Output goes outside the
repository; Lead adjudication follows after both lanes finish.
