# 4F(R1) S5 — Browser hardening stage packet

> **Status:** `S5-P0..P1 PROVED / S5 CLOSED PASS / RECEIPT PUBLISHED`
> **Scope:** complete the accepted T-01/GF-01/W-01 browser boundary over the
> already-realized `S1..S4` operations
> **Prerequisite:** `S4 CLOSED PASS`
> **Execution grant:** operator-approved bounded `S5`; no model/provider execution

## 1. Authority and measurable delivery

This packet compiles only the accepted S5 row in the R1 implementation slice
plan, the locked T-01/GF-01/W-01 Screen Contracts and the preserved P13 inputs.
It creates no Product meaning, operation, Permission, table or durable state.

S5 has two bounded vertical outcomes:

| Packet | Outcome | Measurable progress |
| --- | --- | --- |
| `S5-P0` | the existing S1..S4 browser host completes the locked shell and browser boundary without changing Product contracts | direct refresh of every realized route; one adaptive current-scope navigation; recoverable keyboard focus; exact four-state custody; security headers and reduced-motion behavior |
| `S5-P1` | the complete hardening ceiling is reproducibly falsified across the admitted browser pins | Chromium `1234`, Firefox `1538` and WebKit `2336` each pass the same wide/narrow, focus, accessibility and authority matrix; static bundle/header/secret checks pass |

`S5-P0` is not a new Product surface and does not realize Inception/cognition.
`S5-P1` closes only the engineering boundary over the already-realized human
journeys. No later operation is implied.

## 2. Frozen no-operation/no-table envelope

The canonical 4B wire and generated transport stay byte-exact. PostgreSQL,
migrations, Product operation declarations, generated DTO/routes/clients and
APP-OWNED source receive no S5 mutation.

The complete S5 mutation ceiling is:

```text
apps/hub/src/http/app.ts                         same-origin SPA refresh + security-header realization
apps/web/src/app/shell.tsx                       locked GF-01 adaptive shell/focus realization
apps/web/src/routes/{index,setup,workspace-new,
  workspace-projects,workspace-project-new,
  project-detail,project-baseline-candidate}.tsx exact scope composition and honest states
apps/web/src/features/{identity-access,workspace,project}/components/*
                                                   focus/status/double-command hardening only
apps/web/src/styles.css                           wide/narrow/focus/reduced-motion realization
tests/implementation/r1-s5-browser-hardening.test.mjs
                                                   admitted three-browser behavioral matrix
tests/implementation/r1-s5-browser-boundary.test.mjs
                                                   headers/bundle/route/static structural proof
package.json                                     bounded S5 scripts only
docs/evidence/4f/*s5* + docs/roadmap.md + docs/index.md
                                                   packet/result/status projection
```

A newly proved implementation dependency must reopen this ceiling before
mutation. No package, browser version, route, operation, table, role, schema,
generic store, DTO, assistant or review framework may be added to make proof
pass.

## 3. Exact browser and state boundary

The admitted identities are Playwright `1.62.1`, Chromium revision `1234`
(`151.0.7922.34`), Firefox revision `1538` (`153.0`) and WebKit revision `2336`
(`26.5`). Downloads must match the already-approved Foundation manifest; the
browser name or a locally convenient executable is not sufficient identity.

Only the four accepted client-state classes exist:

```text
SERVER         = Account/session, disclosure, Workspace, Project, candidate/Baseline truth
URL_NAVIGATION = exact owner-issued Workspace/Project/candidate coordinates
FORM_DRAFT     = current unsent human input and same-intake retry material
EPHEMERAL_UI   = menu/drawer/filter/focus/pending/recovery presentation
```

URL, cache, local storage, DOM text, visibility and disabled state never become
authorization, currentness, success or Product identity. Every route and command
re-resolves server truth. A `401` clears protected projections; `403`/`404` do
not become known-empty; dependency failure does not become absence or success.

## 4. Shell and P13 realization ceiling

The browser preserves T-01 as Account-first setup without a Control Plane rail.
After normal session establishment, GF-01 supplies exactly one adaptive
current-scope navigation, human-readable Workspace/Project orientation and the
Account/session disclosure. W-01 keeps Project cards responsive and candidate
and approved Baseline truth distinguishable.

Wide and narrow behavior may change layout only. It may not change reading
order, region priority, primary/secondary action placement, interaction model,
density class, navigation meaning, material information visibility or authority.
The narrow navigation is one drawer, not a second IA. No contextual assistant is
realized because `PRJ-24`/`BLD-16` remain outside S5.

Keyboard focus is visible. Menu/drawer open moves focus into the disclosure;
Escape and explicit close restore the exact trigger. Invalid local input focuses
its labeled control. Pending/result/recovery state is announced without
color-only meaning. Reduced-motion preference disables nonessential animation
without hiding state or controls.

## 5. Frozen RED and deciding-proof matrix

| Protected claim | Required falsifier/proof |
| --- | --- |
| exact three-browser realization | the identical journey/state assertions run under all three admitted engines; a missing/wrong executable or browser omission fails |
| responsive P13 structure | wide and `360x800` narrow paths preserve reading order, one navigation landmark/drawer, candidate access and zero horizontal-scroll dependency |
| focus/accessibility | semantic landmarks/names, labels, current route, live recovery, menu/drawer focus entry, Escape/close restoration and visible `:focus-visible` are asserted |
| reduced motion | emulated `prefers-reduced-motion: reduce` computes no nonessential transition/animation while controls and state remain operable |
| direct-route/server truth | cold refresh of every realized SPA route succeeds through the same-origin host and refetches exact owner responses |
| forged client state resistance | forged URL coordinates, cache, local/session storage and DOM state cannot disclose or promote Workspace/Project/candidate/Baseline truth |
| current session/authority | `401`, revoked/narrowed authority, `403`, non-oracular `404` and dependency failure remove no distinction and retain no protected cached truth |
| double-command resistance | repeated activation while an admitted command is pending emits one request; ambiguous failure retains the same semantic intake/key and never claims success |
| security headers | HTML, static assets and deep-link fallback carry the admitted Helmet policy; API Problems remain non-oracular and no unsafe fallback serves HTML for `/api/*` |
| secret-free bundle | production build contains no configured password, cookie/CSRF value, synthetic secret, private key or server-only environment value |
| generated/owner preservation | canonical wire, generated transport, migrations, operation/Permission/table/role censuses and historical Evidence remain unchanged |
| false-PASS resistance | perturb one browser inclusion, header, secret canary, focus restoration, forged-cache assertion or command counter and observe the exact checker fail |

The proportional deciding floor is S5 structural proof, a production browser
build served by the same-origin Hub boundary, the full three-browser matrix,
the existing import law, `npm ci`, `npm run verify`, native R1C-14 checker,
repository check and preflight. S1..S4 PostgreSQL/Git proofs are reused only
where their subjects and dependency closures remain unchanged.

## 6. Frozen protected-claim and blocker census

Protected claims are exactly:

1. all realized human journeys consume current server truth in the four classes;
2. admitted three-browser wide/narrow/focus/accessibility/reduced-motion parity;
3. direct-route, header and secret-free production-browser boundary;
4. forged client state, narrowed authority and double-command resistance;
5. no operation/table/generated/Product-meaning change and preservation of
   S1..S4 plus historical/native Evidence.

Blocked throughout S5:

- cognition/R1C-13 and `PRJ-07`/`PRJ-24` realization;
- any model/provider call, credential use or real Baseline generation;
- production candidate injection/deployment or external OCI/input custody;
- Product-app scope, contextual/generic assistant or generic review framework;
- any new operation, Permission, table, role, schema, durable state or fifth
  client-state class;
- generic Git/argv/shell authority, external publication, local commit, push,
  PR and merge.

These censuses are frozen. Valid recovery/framework findings that do not
falsify a protected claim are `DEFER SAFELY`. No recursive gate/review expansion
and no additional Fable/AGY/reviewer call are justified unless a material
correction invalidates a protected property or the reliability of deciding
proof.

## 7. Stop conditions

Stop and reopen the smallest owner on client authorization, a fifth state class,
handwritten Product DTO, global business store, route/cache/storage-derived
Product truth, a new operation/table/owner, generic assistant/review machinery,
unsafe security relaxation, secret disclosure or P13 structural regression.
Otherwise execute `S5-P0 → S5-P1` receipt-last without widening.

## 8. Bounded proof-ceiling correction

The final native R1C-14 regression gate correctly failed after S5 changed
inherited `PLATFORM-CONTRACT` paths whose receipt-time transition reasons ended
at S3. This reopens only the proof mutation ceiling for:

```text
scripts/record-r1c14-native-readmission-receipt.mjs
```

The permitted correction is limited to naming the already-authorized S5 reason
for each changed inherited path. It may not change custody membership, prior or
current digests, transition derivation, historical Evidence, OCI identity,
secret checks, Product census or the native PASS criteria. The native checker
and its full targeted suite must pass afterward. Because this correction changes
deciding-proof input classification, it triggers exactly the already-justified
final isolated Fable/AGY closure round after local proof is green.

The correction closed with native `31/31` plus manifest `17/17` PASS. The final
fresh Fable 5.1 and AGY lanes both returned PASS with zero unresolved material
finding; Lead adjudication is recorded in
[`4f-r1-s5-final-independent-review-adjudication.md`](4f-r1-s5-final-independent-review-adjudication.md).
The S5 result receipt is
[`4f-r1-s5-implementation-result.md`](4f-r1-s5-implementation-result.md).
