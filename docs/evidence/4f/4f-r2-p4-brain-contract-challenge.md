# R2-P4 Brain input contract — bounded design challenge

Subject: baseline `23627f5d403336bc23cc789767067903362b46c7` plus the current
roadmap operator amendment and this brief. No implementation change in this
candidate. This is design assistance, not stage-closure convergence. The operator
approved Brain v2 plus a Project-local realization manifest and explicitly
requested Claude Code Fable to help determine the sustainable concrete contract.

## Current question and smallest route

Bootstrap through AGENTS, roadmap, index and Engineering/Repository methods.
Then read stage packet `docs/evidence/4f/4f-r2-stage-code-packet.md` C/C.1/C.2,
`docs/reference/brain-and-knowledge.md` 20.2/20.5/23, Gateway 19.1.1,
`contracts/api/product/project-brain-context-paths.yaml`, and only the relevant
parser/store/ownership consumers: `scripts/bootstrap-r2-brain.mjs`,
`apps/hub/src/brain/store.ts`, `apps/hub/src/gateway/key-conformance.ts`,
Project module/store/recovery and the existing source-import ownership route.
Do not recursively audit historical R1 proofs or unrelated methods.

We need a concrete implementable contract, not another general recommendation
to plan it. Current v1 only contains reviewText and knowledgeBrowse; health
semanticRef has no validated canonical join. PRJ-11 cannot derive conformance
from prose, browse coordinates, qualification success or a caller's PASS.
The registered aggregate evaluator is implemented and tested with synthetic
PostgreSQL, but no real Sankhya mapping/executor is admitted.

## Candidate to challenge, not a preferred verdict

- Preserve immutable v1 and its Workspace reads. Add a closed v2 carrying
  canonical semantic item IDs, explicit browse joins, dependency edges, grain
  and required key-conformance assertions. Reuse existing canonical analytic
  identities if applicable; do not duplicate meaning authority.
- A versioned APP-OWNED `.conexus/brain/realization.json` declares selected
  semantic roots, local registered mappings and exact referenced-source digest
  closure, carried by existing EXISTING_GIT import. PRJ-11 never authors APP
  source. NEW authoring and Builder are not claimed by a test-injected file.
- Brain validates joins/dependency closure/required proof; Project freezes the
  exact inputs and result through the existing recovery protocol. The Gateway
  producer receives existing Connection-use authority, not authority from the
  Brain binding being admitted. No circular Git digest or newer-proof substitution.
- BRN-14 derives only the applicable closure, joins current health, and emits
  opaque Project/binding/item-bound authoring references that grant no rights.

Determine exact minimal fields/invariants, global-vs-selected assertion scope,
empty-vs-missing behavior, health coverage/state handling, invalidation and
proof provenance. Identify which decisions are mechanical under accepted
owners versus genuine missing Product choices. Explicitly challenge whether
the implementation can proceed while the real source is separately qualified;
do not equate controlled proof with real source qualification.

## Falsifiers and proof reconstruction

Two Projects adopt the same immutable Brain revision and health snapshot; only
one realizes its required grain/key condition. They must not both obtain the
same usable context. Attack missing/foreign/caller-forged/stale proof, mutable
binding/source identity, omitted critical dependencies, unjoined health, empty
selection bypass, over-disclosure, stale authoring refs and ownership collisions.
Prior local npm verify, ten evaluator tests, controlled PostgreSQL aggregates
and six recovery boundaries are Evidence for their named modules only.
Inspect test claims; do not rerun expensive proofs or access a provider.

## Output and limits

Return a concise self-contained design recommendation with a concrete field
sketch, the first sustainable executable slice/file envelope and its negative
tests. Classify concrete findings as METHOD FINDING, PRODUCT / PLAN GAP,
LOCAL EXECUTION GAP or NO FINDING, with evidence, failure mode, materiality,
smallest owner, stop/continue and what must not reopen. Keep unknowns unknown;
do not claim a ratified schema, consensus or P4 closure. Lead adjudicates.
No edits, shell mutations, installs, commits, additional reviewers, secrets,
ERP/model calls beyond this authorized review, production effects or new roles.
The user's offer of credentials does not authorize fetching or reading them.
Raw output is retained outside the repo; surviving decisions go into C.1 and
the semantic owners. One focused round; no micro-review loop.
