# 4F(R1) Project cognition — Opus model successor result

> **Status:** `CLOSED PASS / TECHNICAL PROOF COMPLETE / DUAL REVIEW + LEAD
> ADJUDICATION COMPLETE`
> **Subject:** exact Project model successor `claude-opus-5`
> **Receipt SHA-256:**
> `9365c6c8a3cbc34d6761cb368744d551a39d06e98631fb609b943564b0bf25cd`

## Receipt-last result

The operator-authorized diagnostic retry executed the real qualification
transport once with exact model `claude-opus-5`, the fixed synthetic prompt,
zero tools, temperature `0`, existing external personal OAuth custody and no
output-token ceiling.

The resulting receipt is
[`p2-live-opus-successor-receipt.json`](../../../qualification/4f/r1-project-cognition-admission/evidence/p2-live-opus-successor-receipt.json).
It records `PASS`, one HTTPS request to Anthropic `/v1/messages`, zero
unauthorized HTTP/network attempts, an observed response, and no response
content or credential disclosure. No API key participated.

The prior failed attempt produced no PASS receipt. The status-only classifier
remains fail-closed and discloses neither provider body nor credential.

## Verification actually completed

- production OAuth/Opus boundary: `1/1 PASS`;
- qualification OAuth admission: `8/8 PASS`;
- import law: `26/26 PASS`;
- Hub typecheck and changed-file Biome: `PASS`;
- production-composed Chromium/PostgreSQL 17.10 journey: `1/1 PASS` using the
  admitted OCI with ephemeral in-memory database storage;
- repository check, root `npm run verify` and final preflight: `PASS` before
  the receipt-only retry;
- exact live Opus OAuth successor: `PASS`.

The exact composed proof is now recorded receipt-last in
[`4f-r1-project-opus-composed-proof-receipt.json`](4f-r1-project-opus-composed-proof-receipt.json),
SHA-256
`f729ce488b2f705957b06f7231e449ec89aa0267cffa2ee21c5d528baf8698b3`.
It is machine-emitted only after the unchanged composed test returns exact
`1/1 PASS`; it binds its producer, test-output digest, admitted PostgreSQL 17.10
OCI and exact production/test subject hashes. This closes the sole material
Claude finding without another reviewer round.

AGY returned `PASS`. The final Opus confirmation returned `REVISE` only because
the earlier receipt was manually authored; all frozen candidate hashes and the
other six protected claims passed. Lead accepted that finding, executed the
prescribed mechanical receipt producer and adjudicated the successor `CLOSED
PASS` receipt-last.

Post-correction deciding verification in pinned WSL Ubuntu passed: fresh
`npm ci` installed 336 packages with two disclosed low-severity advisories;
`npm run verify` passed with the accepted Redocly warning census; the native
R1C-14 check reconfirmed 17/17 manifest controls and 31/31 targeted tests; the
wrapper plus receipt-producer targeted suite passed 13/13; repository check and
final preflight passed. No full Product/provider call was part of this closure.

## Preserved boundaries

Historical Fable receipts and the historical R1C-13/S6/R1 closure remain
unchanged. Production/multi-user OAuth custody, API keys, fallback/caller model
selection, deployment, next Product tranche, commit, push, PR and merge remain
blocked.
