# 4F(R2) — P4 trusted subject/removal independent review adjudication

> **Combined implementation:** `e3ec7bd..b5e58a4`
> **Proof-hardening successor:** `31bb403`
> **Disposition:** `LOCAL CHECKPOINT ACCEPTED / P4 REMAINS OPEN`
> **Date:** `2026-09-06`

## Result

The trusted Project/Connections subject resolver, controlled registered
observer and PRJ-12 removal/recovery package are accepted as one bounded local
P4 checkpoint. No method finding, Product/plan gap or blocking local execution
gap survives Lead adjudication.

This does **not** close P4. Production Brain-binding routes are not yet composed
in `server.ts`; the exact supported source producer, registered physical read,
mapping, coherent-observation and invalidation contract remain the admitted
P5-owned prerequisite split ahead of P4 production composition. Controlled
observation proves evaluator behavior only and makes no live Sankhya claim.

## Independent Evidence

The first neutral brief froze resolver candidate `b5e58a4` relative to
`f25d48e`. Claude Code `2.1.257`, model `claude-fable-5-1` through alias Fable,
`xhigh`, session `c32c03c6-88fa-471c-9e83-bb8f410b559c`, returned
`VERDICT = CLEAR`. AGY `1.1.27`, `gemini-3.1-pro-high`, `high`, conversation
`9ef548b3-3fd4-433a-aacf-1cf076ed408c`, returned `VERDICT = REVISE` in its
artifact because of one proposed JSON-canonicalization correction.

Fable correctly observed that the first comparison already contained PRJ-12,
while roadmap required the composed removal/resolver package. GPT-6 Astra was
consulted on review proportionality. Lead accepted its recommendation to resume
each isolated lane with one neutral addendum over `e3ec7bd..b5e58a4`, rather
than pay for a duplicate whole review. Each lane retained only its own history.
The addendum produced Fable `CLEAR`; AGY retained only its original
canonicalization objection and otherwise found no removal/composition defect.

AGY's first headless attempts were canceled because its CLI permission matcher
did not disclose the requested command. The successful fresh run and resumed
addendum used a temporary `command(*)` allow entry while retaining `plan`,
sandbox and the no-write review contract. That entry was removed immediately
after each run. The successful conversation, not either canceled attempt, is
the review Evidence.

## Lead adjudication

| Review item | Classification and disposition |
| --- | --- |
| AGY: use RFC-canonical bytes instead of `JSON.stringify` for `sourceScopeId` | **NO FINDING / NO CHANGE REQUIRED.** The ratified v1 contract defines UTF-8 JSON with one exact non-alphabetic key order and no whitespace. The closed object literal plus ECMAScript property enumeration emits those exact bytes, and the deciding test compares the literal byte string. `canonicalBytes` sorts keys lexicographically and produces a different digest, so the proposed change would violate accepted authority. |
| Fable D1: transient binding intent maps to refused | **DEFER SAFELY.** Both outcomes are non-positive. Give the in-progress case a distinct code or propagate indeterminate before PRJ-11 activation, when this classification has a reachable consumer. Owner: Project basis resolver. |
| Fable D2: Connection declaration grammar is duplicated | **DEFER SAFELY.** Divergence fails closed. Converge the shared factory before activation or the next declaration-shape change. Owner: Project. |
| Fable D3: subject PostgreSQL negative matrix is incomplete | **DEFER SAFELY WITH NAMED NEXT PROOF.** Existing owner predicates fail closed and real PostgreSQL covers the principal authority boundaries. Fire active intent, manage revocation, Project-owned foreign scope, malformed revision config and binding/revision environment mismatch in the production-composition proof or before P4 closure. |
| Fable D4: no dedicated command for real composed subject proof | **DEFER SAFELY.** The exact invocation is reproducible and passed without skips. Add the stable command when the P5 producer/composition suite owns its final environment contract. |
| Fable D5: roadmap/index do not route the current subject | **LOCAL EXECUTION GAP / CORRECTED** by this adjudication and the accompanying roadmap/index update. |
| Fable D6: first brief omitted PRJ-12 | **LOCAL EXECUTION GAP / CORRECTED** by the isolated continuity addendum. Both lanes inspected the combined range. |
| Fable addendum A1: addendum wrongly claimed the resolver must refuse after completed removal | **LOCAL CLAIM GAP / CORRECTED HERE.** During a pending removal, the active-intent fence refuses resolution. After completed removal, the resolver remains intentionally independent of Brain binding and may resolve the current Connection/source subject for pre-adoption validation; in-flight source movement becomes indeterminate. The real PostgreSQL subject test establishes a positive result without any `project.brain_binding` row. PRJ-10 and BRN-14 alone become absent/not-found after clear. |
| Fable addendum A2: DELETE-specific negative controls were textual | **LOCAL EXECUTION GAP / CORRECTED** at `31bb403`. Production capability/program controls now fire for nonempty DELETE bytes, absent target and an adulterated non-delete-only APPLY tree; targeted result is `9/9`, zero skips. This test-only strengthening changes no reviewed production property, so another independent round is not justified. |
| Fable addendum A3: intent `declaration_digest` meaning varies by operation kind | **DEFER SAFELY.** Parsing and settlement discriminate the meaning consistently. Add an owner note or normalize the column only with the next intent-schema change; do not rewrite migration `016`. |

## Deciding proof

Lead proof for implementation `b5e58a4` remains:

- clean `npm ci` and clean root `npm run verify`;
- integrated P4 local gate `89` passed, `0` failed, with `12` explicit opt-in
  skips; authority group `29` passed, `0` failed, with `19` environment skips;
- fresh disposable PostgreSQL `17.10` migration, subject-authority and composed
  suites `6/6`, zero skips;
- composed real OCI Git + PostgreSQL + controlled observer passed in `171.7 s`
  inside a `211.5 s` combined run;
- focused pure/regression checks `43` passed, `0` failed; typecheck, import law,
  Biome and diff checks passed;
- proof-hardening successor `31bb403`: targeted PRJ-12 removal `9/9`, zero
  failures/skips, plus Biome and diff check.

The earlier PRJ-12 deciding Evidence remains real PostgreSQL migration `4/4`,
Brain settlement regression `3/3`, real OCI deletion/replay `1/1` in `370.0 s`
and composed PostgreSQL/OCI crash recovery `1/1` in `418.1 s`.

## Current route

```text
P4 OPEN
local resolver/removal checkpoint = ACCEPTED
next = exact P5-owned source-producer admission prerequisite
     + P4 production Brain-binding composition
live Sankhya proof = P7 / separately authorized
```

The next local work may implement configuration, isolated pools, production
composition and targeted HTTP/Git/PostgreSQL proof inside the autonomous-through-
P7 grant. It must not guess the physical Sankhya mapping, registered read,
response/coherence or invalidation semantics, and it must not activate a fixture
observer as production truth. No further generic review round is justified
unless a material correction changes a protected property or deciding proof.
