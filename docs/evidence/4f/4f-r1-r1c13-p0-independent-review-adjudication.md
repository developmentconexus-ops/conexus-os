# 4F(R1) R1C13-P0 — independent closure adjudication

> **Status:** `CLOSED PASS / IN_PROCESS_BOUNDARY_ACCEPTED`
> **Next:** `R1C13-P1`

## Deciding independent round

| Lane | Exact identity | Session | Raw result |
| --- | --- | --- | --- |
| Claude Code Fable | CLI `2.1.257`; normal alias `fable`; canonical `claude-fable-5-1`; `xhigh`; plan/read-only | `9e6a6290-4abc-4da7-ab9a-c4432c2e2afa` | `CLEAR / IN_PROCESS_BOUNDARY_ACCEPTED / P0 NEED NOT STOP`; zero method/Product-plan gap; three non-blocking local gaps routed; one defer-safe item |
| AGY Gemini | CLI `1.1.24`; `gemini-3.1-pro-high`; `high`; plan+sandbox/read-only | `aec6b34f-a916-42b7-89ab-15b897c7b6a7` | `IN_PROCESS_BOUNDARY_ACCEPTED`; `NO FINDING` |

AGY conversation `c4c0c1d7-6d8a-416f-9960-6a8acb2b911b` returned no review
because its read command permission was auto-denied. It is operational Evidence
only and has no deciding weight. The fresh deciding AGY lane received no Fable
output.

## Lead adjudication

All six protected P0 claims survive. The public custom gateway plus exact AI SDK
adapter is accepted as a `TRANSITIONAL SOLUTION`: Mastra remains the cognition
framework; the adapter is internal transport; the decoded response is bounded
before provider parsing; exact origin/redirect denials fire; and the negative
control proves the oversized fixture is not self-failing.

Fable's three local gaps do not block the response-resource claim, but become
explicit P1/S6 obligations:

1. the raw AI SDK error envelope can retain caller prompt values even though the
   bounded transport adds no prompt, response or credential bytes; P1 must
   assert whole-error serialization and S6 must map/discard framework errors
   before any log, Evidence or browser boundary;
2. P1 must set telemetry disabled before dynamic Mastra import, not after a
   hoisted import;
3. P1 must traverse the Mastra Agent/dynamic-model path and prove that only the
   admitted custom gateway resolves a Project model.

The untested SSE path is `DEFER SAFELY`: both admitted R1 profiles use bounded
generate, and no streaming Product claim exists. Revisit only if an accepted
profile begins streaming.

AGY found no additional gap. Fable additionally exercised real loopback fetch,
gzip decoded expansion, chunked overflow, redirect and full error inspection;
those results strengthen but do not replace the committed 7/7 harness.

## Closure

```text
R1C13-P0 = CLOSED PASS
BOUNDARY = PUBLIC IN-PROCESS CUSTOM MASTRA GATEWAY
DIRECT PRODUCT/PROVIDER SDK = FORBIDDEN
BUILT-IN GATEWAYS = STILL BLOCKED / P1 MUST EXCLUDE
P1 = NEXT
P2 / S6 / ROOT PRODUCT DEPENDENCY = BLOCKED
```

No further reviewer call is justified now. Another independent round occurs
only if a material correction invalidates this boundary or at final R1C-13
closure.
